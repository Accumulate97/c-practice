# 题型规范（v3 · 新体系）

> 本文件定义了 C practice 支持的**全部 11 种题型**。
> **主力题型**（占题量 80%）：程序填空、程序改错、程序阅读写结果、编程题
> **辅助题型**（占 20%）：概念选择、判断、概念填空、代码排序、简答、复杂度分析、匹配


---

## 〇、全局强制规则

> **【id 与 category 强制规则】** 唯一真源为 `schema/Problem.schema.json`。
> 所有题目 id **必须带 `c-` 或 `ds-` 前缀**（如 `c-ch09-cc-001`、`ds-ch02-pg-003`），
> 完整模式 `^[cd]s?-ch[0-9]{2}-(cc|dbg|cr|pg|sc|tf|fb|co|sa|cx|mt)-[0-9]{3}$`；
> 并且**必须包含 `category` 字段**（取值 `"c"` 或 `"ds"`）。缺前缀或缺 `category` 均不合法，校验脚本直接 fail。
>
> **【输出比对与末尾换行规则】** `testCases[].expected` 与 `code_reading.answer`
> **统一不写末尾换行**。判分侧必须先归一化再比对：CRLF/CR → LF → 逐行剥离行尾空白 →
> 剥离整体末尾的全部换行 → 逐行比对。与 `05_教材代码风格约定.md`"程序输出末尾必须带 `\n`"并不矛盾：
> 差异由判分侧吸收，不写进数据文件。
---

## 一、题型总览

| 类型 | `type` 值 | 主力 | 在线编译 | 自动判分 |
|---|---|:---:|:---:|:---:|
| 程序填空 | `code_completion` | ★★★★★ | ✅ | ✅ 跑测试用例 |
| 程序改错 | `debug` | ★★★★★ | ✅ | ✅ 跑测试用例 |
| 程序阅读写结果 | `code_reading` | ★★★★★ | ✅ | ✅ 比对输出 |
| 编程题 | `programming` | ★★★★★ | ✅ | ✅ 跑测试用例 |
| 概念选择 | `single_choice` | ★★ | — | ✅ 比对索引 |
| 判断题 | `true_false` | ★★ | — | ✅ 布尔比对 |
| 概念填空 | `fill_blank` | ★★ | — | ✅ 字符串比对 |
| 代码排序 | `code_ordering` | ★★ | ✅ | ✅ 拼接后编译 |
| 简答题 | `short_answer` | ★ | — | ❌ 自评 |
| 复杂度分析 | `complexity` | ★★ | — | ✅ 比对表达式 |
| 匹配题 | `matching` | ★ | — | ✅ 比对配对 |

---

## 二、★ 程序填空 `code_completion`

**与"概念填空"不同**：这是**在一段可编译的程序里挖空**，学生补全后整段代码应能编译通过并通过测试。

### 规范
- 空位用 `/*__BLANK_1__*/` 形式标记（便于程序定位与替换）
- 每个空位提供 `answer`（标准答案）和 `accepted`（可接受的等价写法数组）
- 提供 `solution`（补全后的完整代码）
- 空位数量 1-4 个，不要过多
- **空位必须落在关键知识点上**：循环条件、指针偏移、递归终止条件、边界处理
- 挖空的粒度：一个表达式 / 一个运算符 / 一个语句，不要挖掉半截语法

### 样例

```json
{
  "id": "c-ch09-cc-001",
  "category": "c",
  "type": "code_completion",
  "chapter": "第9章 指针",
  "section": "9.3 指针与数组",
  "source": "原创（依据大纲考点：指针算术与数组遍历）",
  "difficulty": 3,
  "bloom": "apply",
  "stem": "补全程序，使其用指针方式逆序输出数组元素。",
  "code": "#include <stdio.h>\nint main(void) {\n    int a[] = {1,2,3,4,5}, n = 5;\n    int *p = /*__BLANK_1__*/;\n    for (int i = 0; i < n; i++)\n        printf(\"%d \", /*__BLANK_2__*/);\n    return 0;\n}",
  "blanks": [
    {
      "index": 1,
      "hint": "指针应指向数组最后一个元素",
      "answer": "a + n - 1",
      "accepted": ["a+n-1", "&a[n-1]", "a + 4"]
    },
    {
      "index": 2,
      "hint": "每次输出后指针前移",
      "answer": "*p--",
      "accepted": ["*(p--)", "*p--"]
    }
  ],
  "solution": "#include <stdio.h>\nint main(void) {\n    int a[] = {1,2,3,4,5}, n = 5;\n    int *p = a + n - 1;\n    for (int i = 0; i < n; i++)\n        printf(\"%d \", *p--);\n    return 0;\n}",
  "testCases": [{ "stdin": "", "expected": "5 4 3 2 1 " }],
  "explanation": "指针 p 初始指向 a[4]（即 a+n-1）；每次输出 *p 后执行 p-- 使指针前移。注意 *p-- 等价于 *(p--)：先取 p 指向的值，再自减指针。",
  "verified": false
}
```

---

## 三、★ 程序改错 `debug`

### 规范
- 植入 **2-3 个**初学者典型错误
- `bugs` 字段说明错误（**不展示给学生**，仅用于校验与解析）
- `fixed_code` 为修正后代码
- 错误应**分布在不同行**，不要挤在一起

### 初学者 bug 库（务必轮换）
| 类别 | 具体错误 |
|---|---|
| 指针 | 未初始化就解引用；`free` 后继续使用；返回局部变量地址；`sizeof(指针)` 当数组长度 |
| 输入 | `scanf` 漏 `&`；`%c` 前未处理换行；`gets()` 已废弃 |
| 数组 | 越界；字符串忘留 `'\0'`；数组名当左值 |
| 内存 | `malloc` 未检查 NULL；内存泄漏 |
| 运算符 | `=` 与 `==` 混淆；整数除法误用；有符号/无符号混用 |
| 语法 | `if`/`for` 后多余分号；`switch` 漏 `break` |
| 函数 | 值传递误当地址传递；未声明原型 |
| 循环 | 边界差一错误（off-by-one）；死循环 |

### 样例

```json
{
  "id": "c-ch09-dbg-001",
  "category": "c",
  "type": "debug",
  "chapter": "第9章 指针",
  "section": "9.2 指针与函数",
  "source": "原创（依据大纲考点：函数参数传递）",
  "difficulty": 3,
  "bloom": "analyze",
  "stem": "程序意图交换两个整数，但有 2 处错误，改正后应输出 `a=5, b=3`。",
  "code": "#include <stdio.h>\nvoid swap(int x, int y) {\n    int t = x;\n    x = y;\n    y = t;\n}\nint main(void) {\n    int a = 3, b = 5;\n    swap(a, b);\n    printf(\"a=%d, b=%d\\n\", a, b);\n    return 0;\n}",
  "bugs": [
    "swap 参数应为指针类型 int *x, int *y，且函数体内需解引用",
    "调用处应传地址 swap(&a, &b)"
  ],
  "fixed_code": "#include <stdio.h>\nvoid swap(int *x, int *y) {\n    int t = *x;\n    *x = *y;\n    *y = t;\n}\nint main(void) {\n    int a = 3, b = 5;\n    swap(&a, &b);\n    printf(\"a=%d, b=%d\\n\", a, b);\n    return 0;\n}",
  "testCases": [{ "stdin": "", "expected": "a=5, b=3" }],
  "explanation": "C 语言参数按值传递，形参是实参的副本，修改形参不影响实参。需传指针并在函数内解引用。这是初学者最常见的误解之一。",
  "verified": false
}
```

---

## 四、★ 程序阅读写结果 `code_reading`

### 规范
- 给出**完整可编译**程序，要求写出运行结果（或说明功能）
- **答案必须实机运行得到的真实输出**，不得推理想当然
- 优先选择输出反直觉的题目

### 高价值考点（C 语言输出常反直觉）
- `i++` 与 `++i` 混用、未定义行为
- `printf` 参数求值顺序
- 数组名与指针的 `sizeof` 差异
- 整数除法截断、有符号/无符号混用
- `char` 溢出、字符串字面量不可修改
- `scanf` 的 `%c` 与空白符
- 逗号表达式的值
- `strlen` vs `sizeof`
- `*(p++)` vs `(*p)++`
- 静态局部变量、递归
- 结构体内存对齐

### 样例

```json
{
  "id": "c-ch09-cr-001",
  "category": "c",
  "type": "code_reading",
  "chapter": "第9章 指针",
  "section": "9.1 指针运算",
  "source": "原创（依据大纲考点：自增运算符优先级）",
  "difficulty": 3,
  "bloom": "analyze",
  "stem": "阅读程序，写出运行结果。",
  "code": "#include <stdio.h>\nint main(void) {\n    int a[] = {1, 2, 3, 4, 5};\n    int *p = a;\n    printf(\"%d \", *p++);\n    printf(\"%d \", (*p)++);\n    printf(\"%d\\n\", *p);\n    return 0;\n}",
  "answer": "1 2 3",
  "explanation": "`*p++` 等价于 `*(p++)`：后置 ++ 优先级高于 *，先取 *p 的值 1，再使 p 指向 a[1]。`(*p)++`：先取 a[1] 的值 2 输出，随后 a[1] 自增为 3。最后 *p 即 a[1]，输出 3。",
  "verified": false
}
```

### 难度提升变式
可要求"说明程序功能"而非仅输出，此时 `answer` 为文字描述，
需在 `explanation` 中给出逐行分析。

---

## 五、★ 编程题 `programming`

### 规范
- 提供题目、输入格式、输出格式、样例
- **至少 4 组测试用例**，覆盖：典型输入、边界值、极端值、特殊情况
- `code_starter`（留空骨架）+ `reference`（参考实现）

### 样例

```json
{
  "id": "c-ch05-pg-001",
  "category": "c",
  "type": "programming",
  "chapter": "第5章 循环结构",
  "section": "5.3 编程题",
  "source": "变式（源自《C程序设计试题汇编》第5章编程题：完数问题）",
  "difficulty": 3,
  "bloom": "apply",
  "stem": "输入正整数 n，输出 n 以内所有完数（一个数恰好等于它的真因子之和，如 6 = 1 + 2 + 3）。",
  "input_format": "一个正整数 n（1 ≤ n ≤ 10000）",
  "output_format": "每行一个完数，升序；若无则输出 None",
  "code_starter": "#include <stdio.h>\n\nint main(void) {\n    /* 在此编写 */\n    return 0;\n}",
  "reference": "#include <stdio.h>\nint main(void) {\n    int n, i, j, sum, found = 0;\n    scanf(\"%d\", &n);\n    for (i = 2; i <= n; i++) {\n        sum = 1;\n        for (j = 2; j * j <= i; j++)\n            if (i % j == 0) {\n                sum += j;\n                if (j != i / j) sum += i / j;\n            }\n        if (sum == i) { printf(\"%d\\n\", i); found = 1; }\n    }\n    if (!found) printf(\"None\\n\");\n    return 0;\n}",
  "testCases": [
    { "stdin": "10",   "expected": "6",           "note": "典型输入" },
    { "stdin": "1000", "expected": "6\n28\n496", "note": "多结果" },
    { "stdin": "2",    "expected": "None",       "note": "边界：无解" },
    { "stdin": "6",    "expected": "6",          "note": "边界：刚好等于" }
  ],
  "explanation": "枚举 2..n 每个数 i，求真因子和。优化：只需枚举到 sqrt(i)，成对加入因子 j 与 i/j。注意 1 不是完数（真因子不含自身，1 的真因子只有 1 但 1 不算完数）。",
  "verified": false
}
```

---

## 六、辅助题型

> 下列各辅助题型**仅列出该题型特有的字段**；完整题目对象仍须包含第〇节与 `schema/Problem.schema.json` 要求的全部公共字段（含带 `c-`/`ds-` 前缀的 `id` 与 `category`）。


### 概念选择 `single_choice`
4 选项，唯一正确答案，干扰项来自真实误解。禁止"以上都对/都不对"。

### 判断题 `true_false`
```json
{ "stem": "数组名可以作为左值被赋值。", "answer": false,
  "explanation": "数组名是地址常量，不是变量，不能作为左值。" }
```

### 概念填空 `fill_blank`
题干用 `[1]`、`[2]` 标记空位，答案需可精确比对。

### 代码排序 `code_ordering`
打乱代码行顺序，学生拖拽排序。判分：拼接后送 Piston 编译 + 跑测试。
```json
{ "lines": ["int i;", "for(i=0;i<n;i++)", "printf(\"%d\",a[i]);"],
  "correct_order": [0,1,2] }
```

### 简答题 `short_answer`
无法自动判分，提供 `reference_answer` 与 `grading_points`（评分要点数组），学生自评。

### 复杂度分析 `complexity`
给出代码，要求分析时间复杂度/空间复杂度，答案为大 O 表达式。
```json
{ "stem": "分析以下代码的时间复杂度", "code": "...",
  "answer": "O(n^2)", "accepted": ["O(n²)", "O(n*n)"] }
```

### 匹配题 `matching`
左栏概念、右栏定义，学生连线。
```json
{ "pairs": [ {"left":"栈","right":"后进先出"}, {"left":"队列","right":"先进先出"} ] }
```

---

## 七、难度与认知层级

### 难度 `difficulty`（1-5）
| 值 | 含义 |
|---|---|
| 1 | 极简：单个概念直接回忆 |
| 2 | 简单：单个知识点基本应用 |
| 3 | 中等：需 2-3 个知识点组合 |
| 4 | 较难：涉及指针/递归/内存管理 |
| 5 | 困难：综合应用或复杂算法 |

### 认知层级 `bloom`
`remember` / `understand` / `apply` / `analyze`

**约束**：每章题目中 `apply` 及以上**不低于 40%**。

---

## 八、出题硬性禁令

1. 所有 C 代码**必须实机验证** gcc 编译通过
2. 程序阅读题答案必须是**真实运行结果**
3. 不得出现"以上都对""以上都不对"
4. 不得引入大纲外知识点（C++ 特性、POSIX API）
5. 不得生成两道本质重复、只换变量名的题
6. 知识点不足时宁可不生成，不编造
7. **每道题必须有 `explanation` 详解**，解析要讲清"为什么"，不只是"答案是什么"
8. 涉及教材原题变式时，`source` 字段必须注明来源
9. **题目 id 必须带 `c-`/`ds-` 前缀，且必须含 `category` 字段**（见第〇节，Schema 为唯一真源）
10. **`expected` / `answer` 一律不写末尾换行**：换行与行尾空白差异由判分侧归一化吸收（见第〇节）
