# 题型规范（v3 · 新体系）

> 本文件定义了 C practice 支持的**全部 11 种题型**。
> **主力题型**（占题量 80%）：程序填空、程序改错、程序阅读写结果、编程题
> **辅助题型**（占 20%）：概念选择、判断、概念填空、代码排序、简答、复杂度分析、匹配


---

## 〇、全局强制规则

> **【id 与 category 强制规则】** 唯一真源为 `schema/Problem.schema.json`。
> 所有题目 id **必须带 `c-` 或 `ds-` 前缀**（如 `c-ch09-cc-001`、`ds-ch02-pg-003`），
> 完整模式 `^[cd]s?-ch[0-9]{2}-(cc|dbg|cr|pg|sc|tf|fb|co|sa|cx|mt)-[0-9]{3}$`；
> 并且**必须包含 `category` 字段**（取值 `"c"` 或 `"ds"`）。缺前缀或缺 `category` 均不合法，校验脚本直接 fail。
>
> **【输出比对与末尾换行规则】** `testCases[].expected` 与 `code_reading.answer`
> **统一不写末尾换行**。判分侧必须先归一化再比对：CRLF/CR → LF → 逐行剥离行尾空白 →
> 剥离整体末尾的全部换行 → 逐行比对。与 `05_教材代码风格约定.md`"程序输出末尾必须带 `\n`"并不矛盾：
> 差异由判分侧吸收，不写进数据文件。
---

## 一、题型总览

| 类型 | `type` 值 | 主力 | 在线编译 | 自动判分 |
|---|---|:---:|:---:|:---:|
| 程序填空 | `code_completion` | ★★★★★ | ✅ | ✅ 跑测试用例 |
| 程序改错 | `debug` | ★★★★★ | ✅ | ✅ 跑测试用例 |
| 程序阅读写结果 | `code_reading` | ★★★★★ | ✅ | ✅ 比对输出 |
| 编程题 | `programming` | ★★★★★ | ✅ | ✅ 跑测试用例 |
| 概念选择 | `single_choice` | ★★ | — | ✅ 比对索引 |
| 判断题 | `true_false` | ★★ | — | ✅ 布尔比对 |
| 概念填空 | `fill_blank` | ★★ | — | ✅ 字符串比对 |
| 代码排序 | `code_ordering` | ★★ | ✅ | ✅ 拼接后编译 |
| 简答题 | `short_answer` | ★ | — | ❌ 自评 |
| 复杂度分析 | `complexity` | ★★ | — | ✅ 比对表达式 |
| 匹配题 | `matching` | ★ | — | ✅ 比对配对 |

---

## 二、★ 程序填空 `code_completion`

**与"概念填空"不同**：这是**在一段可编译的程序里挖空**，学生补全后整段代码应能编译通过并通过测试。

### 规范
- 空位用 `/*__BLANK_1__*/` 形式标记（便于程序定位与替换）
- 每个空位提供 `answer`（标准答案）和 `accepted`（可接受的等价写法数组）
- 提供 `solution`（补全后的完整代码）
- 空位数量 1-4 个，不要过多
- **空位必须落在关键知识点上**：循环条件、指针偏移、递归终止条件、边界处理
- 挖空的粒度：一个表达式 / 一个运算符 / 一个语句，不要挖掉半截语法

### 样例

```json
{
  "id": "c-ch09-cc-001",
  "category": "c",
  "type": "code_completion",
  "chapter": "第9章 指针",
  "section": "9.3 指针与数组",
  "source": "原创（依据大纲考点：指针算术与数组遍历）",
  "difficulty": 3,
  "bloom": "apply",
  "stem": "补全程序，使其用指针方式逆序输出数组元素。",
  "code": "#include <stdio.h>\nint main(void) {\n    int a[] = {1,2,3,4,5}, n = 5;\n    int *p = /*__BLANK_1__*/;\n    for (int i = 0; i < n; i++)\n        printf(\"%d \", /*__BLANK_2__*/);\n    return 0;\n}",
  "blanks": [
    {
      "index": 1,
      "hint": "指针应指向数组最后一个元素",
      "answer": "a + n - 1",
      "accepted": ["a+n-1", "&a[n-1]", "a + 4"]
    },
    {
      "index": 2,
      "hint": "每次输出后指针前移",
      "answer": "*p--",
      "accepted": ["*(p--)", "*p--"]
    }
  ],
  "solution": "#include <stdio.h>\nint main(void) {\n    int a[] = {1,2,3,4,5}, n = 5;\n    int *p = a + n - 1;\n    for (int i = 0; i < n; i++)\n        printf(\"%d \", *p--);\n    return 0;\n}",
  "testCases": [{ "stdin": "", "expected": "5 4 3 2 1 " }],
  "explanation": "指针 p 初始指向 a[4]（即 a+n-1）；每次输出 *p 后执行 p-- 使指针前移。注意 *p-- 等价于 *(p--)：先取 p 指向的值，再自减指针。",
  "verified": false
}
```

---

## 三、★ 程序改错 `debug`

### 规范
- 植入 **2-3 个**初学者典型错误
- `bugs` 字段说明错误（**不展示给学生**，仅用于校验与解析）
- `fixed_code` 为修正后代码
- 错误应**分布在不同行**，不要挤在一起

### 初学者 bug 库（务必轮换）
| 类别 | 具体错误 |
|---|---|
| 指针 | 未初始化就解引用；`free` 后继续使用；返回局部变量地址；`sizeof(指针)` 当数组长度 |
| 输入 | `scanf` 漏 `&`；`%c` 前未处理换行；`gets()` 已废弃 |
| 数组 | 越界；字符串忘留 `'\0'`；数组名当左值 |
| 内存 | `malloc` 未检查 NULL；内存泄漏 |
| 运算符 | `=` 与 `==` 混淆；整数除法误用；有符号/无符号混用 |
| 语法 | `if`/`for` 后多余分号；`switch` 漏 `break` |
| 函数 | 值传递误当地址传递；未声明原型 |
| 循环 | 边界差一错误（off-by-one）；死循环 |

### 样例

```json
{
  "id": "c-ch09-dbg-001",
  "category": "c",
  "type": "debug",
  "chapter": "第9章 指针",
  "section": "9.2 指针与函数",
  "source": "原创（依据大纲考点：函数参数传递）",
  "difficulty": 3,
  "bloom": "analyze",
  "stem": "程序意图交换两个整数，但有 2 处错误，改正后应输出 `a=5, b=3`。",
  "code": "#include <stdio.h>\nvoid swap(int x, int y) {\n    int t = x;\n    x = y;\n    y = t;\n}\nint main(void) {\n    int a = 3, b = 5;\n    swap(a, b);\n    printf(\"a=%d, b=%d\\n\", a, b);\n    return 0;\n}",
  "bugs": [
    "swap 参数应为指针类型 int *x, int *y，且函数体内需解引用",
    "调用处应传地址 swap(&a, &b)"
  ],
  "fixed_code": "#include <stdio.h>\nvoid swap(int *x, int *y) {\n    int t = *x;\n    *x = *y;\n    *y = t;\n}\nint main(void) {\n    int a = 3, b = 5;\n    swap(&a, &b);\n    printf(\"a=%d, b=%d\\n\", a, b);\n    return 0;\n}",
  "testCases": [{ "stdin": "", "expected": "a=5, b=3" }],
  "explanation": "C 语言参数按值传递，形参是实参的副本，修改形参不影响实参。需传指针并在函数内解引用。这是初学者最常见的误解之一。",
  "verified": false
}
```

---

## 四、★ 程序阅读写结果 `code_reading`

### 规范
- 给出**完整可编译**程序，要求写出运行结果（或说明功能）
- **答案必须实机运行得到的真实输出**，不得推理想当然
- 优先选择输出反直觉的题目

### 高价值考点（C 语言输出常反直觉）
- `i++` 与 `++i` 混用、未定义行为
- `printf` 参数求值顺序
- 数组名与指针的 `sizeof` 差异
- 整数除法截断、有符号/无符号混用
- `char` 溢出、字符串字面量不可修改
- `scanf` 的 `%c` 与空白符
- 逗号表达式的值
- `strlen` vs `sizeof`
- `*(p++)` vs `(*p)++`
- 静态局部变量、递归
- 结构体内存对齐

### 样例

```json
{
  "id": "c-ch09-cr-001",
  "category": "c",
  "type": "code_reading",
  "chapter": "第9章 指针",
  "section": "9.1 指针运算",
  "source": "原创（依据大纲考点：自增运算符优先级）",
  "difficulty": 3,
  "bloom": "analyze",
  "stem": "阅读程序，写出运行结果。",
  "code": "#include <stdio.h>\nint main(void) {\n    int a[] = {1, 2, 3, 4, 5};\n    int *p = a;\n    printf(\"%d \", *p++);\n    printf(\"%d \", (*p)++);\n    printf(\"%d\\n\", *p);\n    return 0;\n}",
  "answer": "1 2 3",
  "explanation": "`*p++` 等价于 `*(p++)`：后置 ++ 优先级高于 *，先取 *p 的值 1，再使 p 指向 a[1]。`(*p)++`：先取 a[1] 的值 2 输出，随后 a[1] 自增为 3。最后 *p 即 a[1]，输出 3。",
  "verified": false
}
```

### 难度提升变式
可要求"说明程序功能"而非仅输出，此时 `answer` 为文字描述，
需在 `explanation` 中给出逐行分析。

---

## 五、★ 编程题 `programming`

### 规范
- 提供题目、输入格式、输出格式、样例
- **至少 4 组测试用例**，覆盖：典型输入、边界值、极端值、特殊情况
- `code_starter`（留空骨架）+ `reference`（参考实现）

### 样例

```json
{
  "id": "c-ch05-pg-001",
  "category": "c",
  "type": "programming",
  "chapter": "第5章 循环结构",
  "section": "5.3 编程题",
  "source": "变式（源自《C程序设计试题汇编》第5章编程题：完数问题）",
  "difficulty": 3,
  "bloom": "apply",
  "stem": "输入正整数 n，输出 n 以内所有完数（一个数恰好等于它的真因子之和，如 6 = 1 + 2 + 3）。",
  "input_format": "一个正整数 n（1 ≤ n ≤ 10000）",
  "output_format": "每行一个完数，升序；若无则输出 None",
  "code_starter": "#include <stdio.h>\n\nint main(void) {\n    /* 在此编写 */\n    return 0;\n}",
  "reference": "#include <stdio.h>\nint main(void) {\n    int n, i, j, sum, found = 0;\n    scanf(\"%d\", &n);\n    for (i = 2; i <= n; i++) {\n        sum = 1;\n        for (j = 2; j * j <= i; j++)\n            if (i % j == 0) {\n                sum += j;\n                if (j != i / j) sum += i / j;\n            }\n        if (sum == i) { printf(\"%d\\n\", i); found = 1; }\n    }\n    if (!found) printf(\"None\\n\");\n    return 0;\n}",
  "testCases": [
    { "stdin": "10",   "expected": "6",           "note": "典型输入" },
    { "stdin": "1000", "expected": "6\n28\n496", "note": "多结果" },
    { "stdin": "2",    "expected": "None",       "note": "边界：无解" },
    { "stdin": "6",    "expected": "6",          "note": "边界：刚好等于" }
  ],
  "explanation": "枚举 2..n 每个数 i，求真因子和。优化：只需枚举到 sqrt(i)，成对加入因子 j 与 i/j。注意 1 不是完数（真因子不含自身，1 的真因子只有 1 但 1 不算完数）。",
  "verified": false
}
```

---

## 六、辅助题型

> 与第二~五节的主力题型样例一样，下列 7 种辅助题型**每类都给出完整可校验的题目对象**
> （含第〇节与 Schema 要求的全部公共字段），可直接通过 `schema/Problem.schema.json` 的 Ajv 校验。
> 校验脚本会扫描本文件**所有** ```json 代码块，任何一块 fail 即视为规范漂移。
> `verified`：非代码题按 Schema 规定固定 `true`；代码类须实机验证通过后才置 `true`。

### 概念选择 `single_choice`
4 选项，唯一正确答案，干扰项来自真实误解。禁止"以上都对/都不对"。

```json
{
  "id": "c-ch09-sc-001",
  "category": "c",
  "type": "single_choice",
  "chapter": "第9章 指针",
  "section": "9.1 指针运算",
  "source": "原创（依据大纲考点：数组名与指针的 sizeof 差异）",
  "difficulty": 2,
  "bloom": "understand",
  "stem": "设有 `int a[10]; int *p = a;`，下列关于 `sizeof` 的说法正确的是（ ）",
  "options": [
    "sizeof(p) 为 40，因为 p 指向含 10 个元素的数组",
    "sizeof(a) 为 40（int 按 4 字节计），sizeof(p) 是指针本身的大小",
    "sizeof(a) 与 sizeof(p) 的值相同",
    "sizeof(p) 只能在运行时求得"
  ],
  "answer": 1,
  "explanation": "数组名 a 直接出现在 sizeof 运算符中时代表整个数组，故 sizeof(a)=10*4=40；而 p 是一个指针变量，sizeof(p) 取的是指针自身的大小（64 位程序下通常为 8），与它指向多少个元素无关。两者都是编译期常量。这正是\"数组名不等于指针\"最典型的考点。",
  "tags": ["指针", "sizeof", "数组名"],
  "verified": true
}
```

### 判断题 `true_false`
题干写成可判真伪的陈述句，`answer` 为布尔值。

```json
{
  "id": "c-ch09-tf-001",
  "category": "c",
  "type": "true_false",
  "chapter": "第9章 指针",
  "section": "9.3 指针与数组",
  "source": "原创（依据大纲考点：数组名的左值属性）",
  "difficulty": 2,
  "bloom": "understand",
  "stem": "数组名可以作为左值被赋值。",
  "answer": false,
  "explanation": "数组名是地址常量而非变量，不能作为左值：`a = b;` 会报 assignment to expression with array type。可以给数组元素赋值，也可以让指针指向别处，但不能整体改变数组名指向；要复制数组须逐元素赋值，或用 strcpy / memcpy。",
  "tags": ["数组名", "左值"],
  "verified": true
}
```

### 概念填空 `fill_blank`
题干用 `[1]`、`[2]` 标记空位，答案需可精确比对；同义写法一律放进 `accepted`。

```json
{
  "id": "ds-ch01-fb-001",
  "category": "ds",
  "type": "fill_blank",
  "chapter": "第1章 绪论",
  "section": "1.2.2 数据结构",
  "source": "原创（依据大纲考点：逻辑结构的四分类）",
  "difficulty": 1,
  "bloom": "remember",
  "stem": "数据的逻辑结构分为四类：集合、[1] 结构、[2] 结构和 [3] 结构。",
  "blanks": [
    {
      "index": 1,
      "answer": "线性",
      "accepted": ["线性表"]
    },
    {
      "index": 2,
      "answer": "树形",
      "accepted": ["树"]
    },
    {
      "index": 3,
      "answer": "图形",
      "accepted": ["网状", "图"]
    }
  ],
  "explanation": "逻辑结构只描述元素之间的关系，与存储无关：集合（同属一集，无其他关系）、线性结构（一对一）、树形结构（一对多）、图形/网状结构（多对多）。答题时\"图形\"与\"网状\"等同义写法都算对，因此必须给出 accepted 备选。",
  "tags": ["逻辑结构", "基本概念"],
  "verified": true
}
```

### 代码排序 `code_ordering`
打乱代码行顺序，学生拖拽排序。`lines` 为**打乱后呈现给学生**的行，`correct_order` 给出正确顺序（元素是 `lines` 的下标）；`lines` 拼接成完整程序后送在线判分后端编译 + 跑测试（后端见 `docs/adr/0001-judge-backend.md`）。

```json
{
  "id": "c-ch05-co-001",
  "category": "c",
  "type": "code_ordering",
  "chapter": "第5章 循环结构",
  "section": "5.3 编程题",
  "source": "原创（依据大纲考点：for 语句累加求和）",
  "difficulty": 2,
  "bloom": "apply",
  "stem": "把下列被打乱的程序行恢复成正确顺序，使程序读入正整数 n 后输出 1+2+...+n 的值。",
  "lines": [
    "    s = s + i;",
    "int main(void) {",
    "    return 0;",
    "#include <stdio.h>",
    "    int n, i, s = 0;",
    "    printf(\"%d\\n\", s);",
    "    scanf(\"%d\", &n);",
    "}",
    "    for (i = 1; i <= n; i++)"
  ],
  "correct_order": [3, 1, 4, 6, 8, 0, 5, 2, 7],
  "testCases": [
    {
      "stdin": "100",
      "expected": "5050",
      "note": "典型输入"
    },
    {
      "stdin": "1",
      "expected": "1",
      "note": "边界：n=1"
    }
  ],
  "explanation": "顺序为：包含头文件 → 定义 main → 声明变量 → 读入 n → for 循环 → 循环体累加 → 输出 → return 0 → 收尾大括号。两个易错点：变量必须先声明后使用；for 只控制紧随其后的一条语句，所以 s = s + i; 必须在 printf 之前、且不能被输出行打断。若把 printf 排进循环体内，输出会变成 10 行累加过程而不是一个结果。",
  "verified": true
}
```

### 简答题 `short_answer`
无法自动判分，提供 `reference_answer` 与 `grading_points`（评分要点数组），学生自评。

```json
{
  "id": "ds-ch02-sa-001",
  "category": "ds",
  "type": "short_answer",
  "chapter": "第2章 线性表",
  "section": "2.6 顺序表和链表的比较",
  "source": "原创（依据大纲考点：顺序表与链表的时间性能比较）",
  "difficulty": 3,
  "bloom": "analyze",
  "stem": "比较顺序表与单链表在「按位访问元素」和「插入/删除元素」两类操作上的时间复杂度，并各举一个适合顺序表、一个适合单链表的应用场景。",
  "reference_answer": "按位访问：顺序表 O(1)（下标直接算地址），单链表 O(n)（必须从头结点了数）。插入/删除：顺序表平均需移动约一半元素，O(n)；单链表在已持有前驱结点指针时改指针即可，O(1)，但若从表头先定位第 i 个位置，总代价仍是 O(n)。顺序表适合元素个数基本固定、以随机访问和顺序遍历为主的场合，如学生成绩表、静态查找表；单链表适合长度变化大、频繁在表头或中部插删的场合，如操作系统空闲分区链、多项式相加。",
  "grading_points": [
    "顺序表按位访问 O(1)，单链表 O(n)",
    "顺序表插删需移动元素，均摊 O(n)",
    "链表插删 O(1) 的前提是已取得前驱结点，需说明是否含定位开销",
    "两个场景须体现各自的存储特征，不能两者通用"
  ],
  "explanation": "本题真正的分界线是有没有把「定位」算进代价：很多人写成「链表插删就是 O(1)」，忽略了先找位置的那次遍历。反过来，顺序表的连续存储在缓存友好性上占优，n 较小时实测常数比链表小得多——这也是「时间复杂度相同不等于速度相同」的一个例子。",
  "tags": ["顺序表", "单链表", "性能比较"],
  "verified": true
}
```

### 复杂度分析 `complexity`
给出代码，要求分析时间/空间复杂度，答案为大 O 表达式；`code` 可以是函数体内的代码段，不必是完整程序。

```json
{
  "id": "ds-ch01-cx-001",
  "category": "ds",
  "type": "complexity",
  "chapter": "第1章 绪论",
  "section": "1.4.3 算法的时间复杂度",
  "source": "原创（依据大纲考点：多重循环的基本运算次数）",
  "difficulty": 3,
  "bloom": "analyze",
  "stem": "分析以下代码段的时间复杂度（以 n 为问题规模，基本运算为 c++）。",
  "code": "int i, j, k, c = 0;\\nfor (i = 1; i <= n; i++)\\n    for (j = 1; j <= i; j++)\\n        for (k = 1; k <= j; k++)\\n            c++;",
  "answer": "O(n^3)",
  "accepted": ["O(n*n*n)", "O(n³)", "O(n^3)"],
  "explanation": "c++ 的执行次数为 sum(i=1..n) sum(j=1..i) j = sum(i=1..n) i(i+1)/2 = n(n+1)(n+2)/6，最高次项为 n^3/6，按大 O 定义去掉常数与低次项得 O(n^3)。若题目改问 sum 为 n(n+1)(n+2)/6 的精确值，则属于另一个考点。注意不能只数「有几层循环」：即使某层循环上限是常数，层数多也不代表同阶。",
  "tags": ["时间复杂度", "多重循环"],
  "verified": true
}
```

### 匹配题 `matching`
左栏概念、右栏定义，学生连线。右栏描述必须唯一，不得出现一左多右。

```json
{
  "id": "ds-ch03-mt-001",
  "category": "ds",
  "type": "matching",
  "chapter": "第3章 栈和队列",
  "section": "3.1 栈和队列的定义和特点",
  "source": "原创（依据大纲考点：栈与队列的定义及循环队列）",
  "difficulty": 1,
  "bloom": "remember",
  "stem": "把左侧的术语与右侧的定义或特性连线。",
  "pairs": [
    {
      "left": "栈",
      "right": "只在表尾一端进行插入和删除的线性表"
    },
    {
      "left": "队列",
      "right": "在表尾插入、在表头删除的线性表"
    },
    {
      "left": "top",
      "right": "指示栈顶元素位置，随进栈出栈改变"
    },
    {
      "left": "假溢出",
      "right": "队尾指针已到数组末尾而队头仍有空闲空间"
    },
    {
      "left": "循环队列",
      "right": "对下标取模使数组首尾相接，用以消除假溢出"
    }
  ],
  "explanation": "五个术语分别来自 3.1 与 3.5.2：栈是后进先出（LIFO），其操作端由 top 标记；队列是先进先出（FIFO），操作端分为队头 front 与队尾 rear。顺序队列因为只增不减下标，会出现「数组没满却报满」的假溢出，循环队列用 (rear+1) % MaxSize 把数组在逻辑上接成环来解决。出题时右侧描述必须唯一匹配，否则会出现在同一右侧项上两解。",
  "tags": ["栈", "队列", "循环队列"],
  "verified": true
}
```

---

## 七、难度与认知层级

### 难度 `difficulty`（1-5）
| 值 | 含义 |
|---|---|
| 1 | 极简：单个概念直接回忆 |
| 2 | 简单：单个知识点基本应用 |
| 3 | 中等：需 2-3 个知识点组合 |
| 4 | 较难：涉及指针/递归/内存管理 |
| 5 | 困难：综合应用或复杂算法 |

### 认知层级 `bloom`
`remember` / `understand` / `apply` / `analyze`

**约束**：每章题目中 `apply` 及以上**不低于 40%**。

---

## 八、出题硬性禁令

1. 所有 C 代码**必须实机验证** gcc 编译通过
2. 程序阅读题答案必须是**真实运行结果**
3. 不得出现"以上都对""以上都不对"
4. 不得引入大纲外知识点（C++ 特性、POSIX API）
5. 不得生成两道本质重复、只换变量名的题
6. 知识点不足时宁可不生成，不编造
7. **每道题必须有 `explanation` 详解**，解析要讲清"为什么"，不只是"答案是什么"
8. 涉及教材原题变式时，`source` 字段必须注明来源
9. **题目 id 必须带 `c-`/`ds-` 前缀，且必须含 `category` 字段**（见第〇节，Schema 为唯一真源）
10. **`expected` / `answer` 一律不写末尾换行**：换行与行尾空白差异由判分侧归一化吸收（见第〇节）
