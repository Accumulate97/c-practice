# C practice —— 项目任务书（v3）

> **使用方式**：把本文件整体复制粘贴给 Codex，它会自动读取同目录下的其他素材文件。
> 第一轮对话后等它输出方案，你确认后再进入实施阶段。

---

## 一、项目定位

**C practice** = C 语言 + 数据结构的**一站式在线学习平台**。

不是单纯的刷题站，而是三大板块联动：

| 板块 | 作用 | 用户心智 |
|---|---|---|
| 📖 **知识点汇总** | 学 | "这个概念是什么" |
| 🎬 **可视化演示** | 懂 | "它是怎么工作的" |
| ✍️ **在线刷题** | 练 | "我会不会用" |

三者通过 `knowledgeIds` / `vizIds` / `relatedProblems` **双向关联**：
知识卡片 → 跳转演示 → 跳转题目 → 回指知识卡片。

**最终开源到 GitHub，用 GitHub Pages 公开访问。**

---

## 二、硬性技术约束（不可违反）

1. **纯静态、无后端**
   - 必须能部署在 GitHub Pages
   - 不得引入任何需要服务端运行时的方案
   - 用户进度用 `localStorage` 保存

2. **在线编译引擎：Piston 公共 API**
   - 端点：`POST https://emkc.org/api/v2/piston/execute`
   - 免 API Key，支持 C（`language: "c"`）
   - 限制：**5 次/秒**，前端必须做请求节流与排队
   - 把编译层抽象成独立模块，预留切换到 Judge0 的接口

3. **题目代码必须是标准 C，且实机验证**
   - 严禁使用严蔚敏教材的"类C伪码"（`&` 引用、`new`/`delete`、`cin`/`cout`）
   - 所有代码类题目必须通过 Piston 真实编译运行
   - 未验证的题目 `verified` 字段必须为 `false`

4. **数据以 JSON 文件形式存放**，按章节分片 + 索引文件，前端懒加载

---

## 三、素材文件（同目录下，请先全部读取）

| 文件 | 内容 | 优先级 |
|---|---|---|
| `01_知识大纲_C语言.md` | 谭浩强《C程序设计》10 章 + C Primer Plus 17 章完整目录 | ★★★★ |
| `02_知识大纲_数据结构.md` | 严蔚敏 8 章（165 条目，主骨架）+ 朱战立 11 章 + 大话 9 章 | ★★★★ |
| `03_真实题目原文.md` | 《C程序设计试题汇编》1007 题的**原题全文** + 命题规律 | ★★★★★ |
| `04_题型规范与样例.md` | **11 种题型**的 JSON 定义与样例（主力 4 种） | ★★★★★ |
| `05_教材代码风格约定.md` | 教材代码风格对比 + 判题必须遵守的标准 C 规范 | ★★★★ |
| `06_变式出题方法论.md` | **如何把教材内容变式为新题**的 7 种手法 | ★★★★ |
| `07_可视化演示规范.md` | 可视化演示清单、交互规范、技术方案 | ★★★★★ |
| `08_知识点汇总规范.md` | 知识库内容清单、卡片结构、呈现要求 | ★★★★★ |
| `schema/Problem.schema.json` | 题目 JSON Schema（v3，11 种题型） | ★★★★ |
| `schema/Knowledge.schema.json` | 知识点卡片 JSON Schema | ★★★★ |

### 阅读顺序（第一轮请按此顺序读）
1. `00` 本文件 —— 明确目标与约束
2. `08` + `07` —— 理解知识库与可视化两大新板块
3. `04` + `06` —— 掌握题型与出题方法
4. `03` —— 建立"什么算好题"的质量直觉
5. `01` / `02` / `05` —— 对照大纲与代码规范

### ⚠️ 关于教材正文
部分教材 PDF 的文字层存在 OCR 损伤
（如 `typedef stru'ot` 实为 `typedef struct`），
因此**不提供损坏的正文全文**。
出题时以大纲确定章节，用你自身的 C 语言知识生成正确代码。

---

## 四、功能要求

### A. 📖 知识点汇总板块
详见 `08_知识点汇总规范.md`

1. 三级导航树（C语言/数据结构 → 章 → 节），可折叠可搜索
2. 全局全文搜索（标题 + 正文 + 代码）
3. 知识卡片：定义、要点清单、易错点、**可运行示例代码**
4. 语法速查表（运算符优先级、关键字、格式说明符、库函数索引）
5. 数据结构对比表格（顺序表 vs 链表、**排序算法总表**、查找算法对比等）
6. 复杂度速查 + 增长曲线
7. **示例代码一键运行**（调用 Piston）
8. 与演示、题目的三向跳转
9. 已读标记（localStorage）

### B. 🎬 可视化演示板块
详见 `07_可视化演示规范.md`

**必须实现**：
- 线性表：单链表、双向链表、循环链表（插入/删除/查找）
- 栈与队列：顺序栈、链栈、循环队列、括号匹配、表达式求值
- 树：四种遍历、**AVL 四种旋转**、BST 插入删除、哈夫曼构造
- 图：DFS、BFS、Prim、Kruskal、Dijkstra、拓扑排序
- 排序：冒泡、选择、插入、希尔、归并、快速、堆、基数（**含多算法对比模式**）
- 查找：顺序、折半、BST、散列表
- 串：KMP（含 next 数组推导）
- **C 语言特有**：指针与内存（指针运算、多级指针、函数调用栈、值传递 vs 地址传递、malloc/free）

**统一交互**：播放/暂停、单步前进/后退、速度调节(0.5x~4x)、重置、
随机数据/手动输入、步骤解说、代码同步高亮

### C. ✍️ 在线刷题板块
1. 在线代码编辑器：语法高亮、行号、Tab 缩进
2. 一键编译运行，展示 stdout/stderr/编译错误/退出码
3. 自定义 stdin
4. 编译选项 `-Wall -Wextra` 开关
5. 按章节/难度/题型筛选
6. 刷题模式：顺序 / 随机 / 只看错题
7. 提交后即时反馈，**每道题都有详解**（`explanation`，默认折叠）
8. 编程类题目自动判分：跑全部测试用例，逐组显示通过情况 + 输出 diff
9. 错题本、收藏、进度统计（正确率、按章节掌握度）
10. 进度导出为 JSON

### D. 全站
11. 暗色/亮色主题切换
12. 响应式布局（桌面 + 平板）
13. 首页：三大板块入口 + 学习路径引导 + 进度概览

---

## 五、题型体系（重点变化）

**主力题型（占题量 80%）**：
1. **程序填空** `code_completion` —— 可编译代码中挖空，补全后跑测试
2. **程序改错** `debug` —— 植入 2-3 个典型错误
3. **程序阅读写结果** `code_reading` —— 写真实运行结果
4. **编程题** `programming` —— 至少 4 组测试用例

**辅助题型（占 20%）**：
5. 概念选择 `single_choice`
6. 判断题 `true_false`
7. 概念填空 `fill_blank`
8. 代码排序 `code_ordering`
9. 复杂度分析 `complexity`
10. 简答题 `short_answer`
11. 匹配题 `matching`

格式详见 `04_题型规范与样例.md`，Schema 见 `schema/Problem.schema.json`。

**所有题目必须带 `explanation` 详解。**

**题目来源**：依据 `06_变式出题方法论.md`，从教材知识点变式生成，
`source` 字段如实注明出处。

---

## 六、第一轮请你输出（先不要写代码）

阅读全部素材后，只输出**技术方案**，不要创建任何文件：

1. **技术栈选型**
   - 前端框架（建议 Vite + React + TypeScript）
   - 代码编辑器组件（CodeMirror 6 / Monaco / Ace，说明体积影响）
   - 可视化方案（建议 SVG + React，不用 D3 的理由）
   - 搜索方案（建议 MiniSearch 等纯前端库）
   - 状态管理、路由、UI 库
   - 说明为什么这套选型适合「纯静态 + GitHub Pages + 无后端」

2. **完整目录结构**（含 viz / knowledge / problems 三大模块）

3. **数据组织与懒加载策略**
   - 题目、知识点、演示的索引与分片结构
   - 数百道题 + 数百张卡片的按需加载方案

4. **编译模块设计**
   - Piston API 封装
   - 5次/秒限流的节流与队列
   - 超时、错误、并发控制

5. **可视化模块的核心抽象**
   - Step / Player / Renderer 的类型设计
   - 算法生成器与渲染层如何解耦

6. **分阶段实施计划**
   - 拆成 8-10 个可独立验证的阶段
   - 每阶段产出什么、如何验证
   - 建议：先骨架 + 编辑器 + 编译，再知识库，再可视化，最后批量出题

7. **你认为最容易出问题的 3 个点 + 规避方案**

---

## 七、后续协作规则

1. **每个阶段开始前**，先用 3 句话说明要改动哪些文件、各负责什么，我确认后再动手
2. **不要改动上一阶段已验证通过的文件**，确需改动先说明原因
3. **每个阶段结束**，告诉我如何本地启动验证（含命令）
4. 所有提交信息用中文

---

## 八、出题阶段（骨架完成后）

按章节推进，每次一章，**单次 12-15 道**：

- 先读 `03` 中对应章节原题（若有），对齐风格与难度
- 按 `06` 的变式手法生成新题
- 严格遵循 `04` 的 JSON 格式与 `schema/Problem.schema.json`
- 题型配比建议：**程序填空 3 : 改错 3 : 阅读 3 : 编程 3 : 其他 3**
- 每章 `apply` 及以上 Bloom 层级**不低于 40%**
- 存到 `src/data/problems/{c|ds}-chXX_主题.json`
- 生成后**立即**用 Piston 批量验证，失败的标出修正，通过才置 `verified: true`

### C 语言推进顺序（权重依据试题汇编篇幅）
数据类型与运算符 → 控制结构 → 数组与字符串 → 函数
→ **指针（25% 题量）** → 结构体与文件 → 预处理与位运算

### 数据结构推进顺序（严蔚敏体系）
绪论与复杂度 → 线性表 → 栈与队列 → 串与数组
→ 树与二叉树 → 图 → 查找 → 排序
