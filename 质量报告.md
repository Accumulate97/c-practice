# PDF 检测报告（v2 重新扫描）

检测时间：2026-09-05
检测工具：PyMuPDF（文字层探测与书签提取）、pdfinfo（元信息）、tesseract（OCR 可行性验证）

---

## 一、总览（9 份文件）

| # | 文件 | 页数 | 文字层 | 书签 | 判定 | 提取结果 |
|---|---|---|---|---|---|---|
| 1 | **C程序设计试题汇编**（谭浩强） | **328** | 无 | 322 | 纯扫描 | ✅ **目录 + 原题全文** |
| 2 | 数据结构C语言版第2版（严蔚敏） | 274 | 有 | 166 | 正文可读 | ✅ **165 条目录 + 第1章正文** |
| 3 | 数据结构·使用C语言第5版（朱战立） | 321 | 无（仅末页） | 0 | 纯扫描 | ✅ 11 章目录 + 第1章正文 |
| 4 | C程序设计（谭浩强·第五版） | 395 | 无 | 219 | 纯扫描 | ✅ 目录 208 条 |
| 5 | 大话数据结构 | 464 | 有（劣质OCR） | 113 | OCR 文本层 | ✅ 目录 111 条 |
| 6 | C Primer Plus 第6版 | 1482 | 有（CID乱码） | 549 | 中文不可恢复 | ✅ 目录 541 条 |
| 7 | C PRIMER PLUS 第6版 习题解答 | 1482 | 有（CID乱码） | 549 | 中文不可恢复 | 与 #6 同文件（字节数完全相同） |
| 8 | 数据结构++C语言版++第2版 | 274 | 有（OCR） | 166 | 与 #2 同文件 | 重复，已并入 #2 |
| 9 | 30352985.pdf | 17 | 无 | 0 | 试题汇编残本 | ⚠️ 已被 #1 取代 |

**去重后有效教材 6 本。**

---

## 二、关键发现

### 发现 1：试题汇编是完本（v1 误判为残本）

v1 检测到的 `30352985.pdf` 只有 17 页，判定为残本。
v2 中新上传的 `C程序设计试题汇编 (谭浩强).pdf` **328 页，90MB**，
与原书"12 章 1007 题"的规模吻合。

书签 322 条，但均为页码（"1"、"2"……"317"），无章节文字。
真正的章节信息来自目次页解析，题目原文来自正文提取。

**这是本项目价值最高的素材**——题型、难度梯度、干扰项设计均由专业教师团队完成。

### 发现 2：严蔚敏教材用的是"类C伪码"，不可直接编译

教材明确说明采用"介于伪码和 C 语言之间的类C语言"，包含：
- 引用参数 `Complex &C`（C++ 特性）
- `new` / `delete` 动态内存（C++ 特性）
- `cin` / `cout` 输入输出（C++ 特性）
- 省略变量声明、`<-->` 交换赋值等伪码写法

**影响**：若直接把这些伪码用于在线判题，gcc 会编译失败。
已在 `05_教材代码风格约定.md` 中明确禁止，并给出标准 C 的改写方式。

### 发现 3：朱战立教材是标准 C，可直接作为代码风格参考

该教材声明"所有算法都经上机调试通过"，正文中的代码为标准 C：
```c
void Reverse(DataType a[], int n)
{
    int i, m = n/2;
    DataType temp;
    for(i = 0; i < m; i++)
    {
        temp = a[i];
        a[i] = a[n-1-i];
        a[n-1-i] = temp;
    }
}
```
**出题代码风格应对齐这本。**

### 发现 4：两本"不同"的书是同一个文件

`C PRIMER PLUS中文版第6版.pdf` 与 `C PRIMER PLUS中文版习题解答 第6版.pdf`
字节数均为 17,973,504，MD5 应相同——是同一文件改了名。

同理 `数据结构C语言版第2版.pdf` 与 `数据结构++C语言版++第2版.pdf`
字节数均为 40,456,708，也是同一文件。

### 发现 5：C Primer Plus 的中文 CID 乱码不可恢复

实测第 61 页：
```
PyMuPDF  : 学习C䈝䀰之前，先介绍一下本书ⲴṬᔿ
pdfplumber: 本书用(cid:12971)(cid:1388)(cid:3416)屏幕上
```
两种提取器都不行——PDF 的 ToUnicode 映射表缺失，属于文件本身缺陷。
但**英文代码完好**：`printf("Concrete contains gravel and cement.\n");`

---

## 三、OCR 可行性验证

### 环境限制
- CPU 3 核 / 内存 4GB / **无 GPU**
- 可用磁盘 464MB
- MinerU、Marker 等深度学习 OCR 需约 2GB 模型权重，**无法部署**

### 实测数据
- tesseract 5 已装，含 `chi_sim` 中文包
- 5 页 OCR 耗时 8.4s → 274 页约 7.7 分钟，464 页约 13 分钟
- 但 OCR 对 C 代码存在**结构性缺陷**：
  - 缩进层级丢失
  - `{}[]*&->` 等符号易错
  - `l`/`1`/`I`、`0`/`O` 混淆

### 实测对比（大话数据结构）
| 方法 | 结果 |
|---|---|
| 原文字层 | `typedef stru'ot DulNode`、`ElemType dat事J` |
| pymupdf4llm 重跑 OCR | `iEf富以一种趣 l床方式`、`讲解放据结构` |

**结论：不做全量 OCR，产物不可用。**

### 提取器性能对比
| 提取器 | 274 页耗时 | 说明 |
|---|---|---|
| pymupdf4llm（自动 OCR） | 约 461s | 质量差 |
| PyMuPDF 纯文字层 | **12.1s** | 快 38 倍 |

---

## 四、最终处理策略

### 提供
| 内容 | 来源 | 规模 |
|---|---|---|
| C 语言章节大纲 | 谭浩强 10 章 + C Primer Plus 17 章完整目录 | 约 400 条目 |
| 数据结构章节大纲 | 严蔚敏 165 条目 + 朱战立 11 章 + 大话 9 章 | 约 350 条目 |
| **真实题库原文** | 试题汇编第 1-2 章原题全文 | 约 45 道 |
| 命题规律分析 | 从原题提炼 | 6 条规律 |
| 题型规范与 JSON 样例 | 人工编写 | 5 种题型 |
| 教材代码风格约定 | 三本教材对比 | — |
| 题目 JSON Schema | 人工编写 | — |

### 不提供
- ❌ 损坏的 OCR 正文全文
- ❌ CID 乱码的中文文本
- ❌ 重复的 PDF（#7、#8）
- ❌ 17 页残本（#9）

### 收益
1. 规避错误代码导致的错误答案
2. 上下文从数百万字符降至约 8 万字符，token 成本降低两个数量级
3. 真实题库提供了「质量标准」，Codex 无需猜测出题尺度
4. 明确了「标准 C vs 类C伪码」的边界，避免生成无法编译的题目
