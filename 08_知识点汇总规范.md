# 知识点汇总规范

> C practice 的第二大板块：**系统的知识库**，与刷题、可视化并列为三大支柱。
> 要求：打开就能直观看到，可检索，示例代码可直接运行。

---

## 一、定位

| 板块 | 作用 | 用户心智 |
|---|---|---|
| **知识点汇总** | 学 | "这个概念是什么" |
| **可视化演示** | 懂 | "它是怎么工作的" |
| **在线刷题** | 练 | "我会不会用" |

三者通过 `knowledgeId` 与 `vizId` 双向关联：
知识点卡片 → 跳转到相关演示 → 跳转到相关题目。

---

## 二、内容结构

按 `01_知识大纲_C语言.md` 与 `02_知识大纲_数据结构.md` 的章节体系组织，
**一个末级章节条目 = 一张知识点卡片**。

### 知识点卡片的标准结构

```typescript
interface Knowledge {
  id: string;              // 如 "c-pointer-arith"
  title: string;           // 指针的算术运算
  category: 'c' | 'ds';    // C 语言 / 数据结构
  chapter: string;         // 第9章 指针
  section: string;         // 9.3 指针运算
  order: number;           // 章节内排序

  summary: string;         // 一句话定义（列表页展示）
  content: string;         // 正文（Markdown，支持代码高亮）
  keyPoints: string[];     // 要点清单（3-6 条）
  pitfalls: string[];      // 易错点（2-4 条）

  examples?: Example[];    // 代码示例（可运行）
  tables?: MarkdownTable[]; // 对比表格（如运算符优先级）
  complexity?: ComplexityInfo; // 数据结构：复杂度信息

  relatedViz?: string[];   // 关联的可视化演示 id
  relatedProblems?: string[]; // 关联的题目 id
  relatedKnowledge?: string[]; // 前置/后续知识点
}
```

### 代码示例结构

```typescript
interface Example {
  title: string;
  code: string;            // 完整可编译代码
  runnable: boolean;       // 是否可在站内运行（默认 true）
  stdin?: string;          // 预设输入
  expected?: string;       // 预期输出（用于对照）
  note?: string;           // 代码讲解
}
```

---

## 三、C 语言知识库的内容清单

### 必含板块

#### 1. 语法速查（可折叠、可搜索）
- 关键字表（32 个 C 关键字 + C99/C11 新增）
- 运算符优先级与结合性**完整表**（`01` 大纲附录 B.2 有原始表格）
- 数据类型与取值范围表
- 转义字符表
- printf/scanf 格式说明符表
- 常用库函数索引（stdio.h / string.h / stdlib.h / math.h / ctype.h）

#### 2. 核心概念详解
按大纲章节，每节一张卡片：
- 定义 + 要点清单
- 可运行示例代码
- 易错点提示
- 配套题目链接

#### 3. 专题深入
| 专题 | 内容 |
|---|---|
| **指针**（最重要） | 指针是什么、指针运算、指针与数组、指针与函数、多级指针、函数指针、动态内存 |
| 数组与字符串 | 一维/二维、字符数组与字符串、常用字符串函数实现原理 |
| 函数 | 参数传递机制、递归、存储类别、作用域与链接 |
| 结构体与共用体 | 内存对齐、位字段、结构体传参 |
| 文件 | 文本文件 vs 二进制、缓冲区、随机访问 |
| 预处理 | 宏的展开规则、条件编译、头文件保护 |

#### 4. 常见错误手册
独立板块，收集 C 语言典型错误（与改错题的 bug 库共用）：
每个错误 = 错误代码 + 现象 + 原因 + 正确写法。

---

## 四、数据结构知识库的内容清单

### 必含板块

#### 1. 概念卡片
每个数据结构一张：定义、ADT、逻辑结构、存储结构、基本操作、应用场景。

#### 2. 算法卡片
每个算法一张：思想、步骤、代码、复杂度、稳定性、适用场合。

#### 3. 对比表格（重点，学生最需要）
| 表格 | 内容 |
|---|---|
| 顺序表 vs 链表 | 空间/时间/适用场景全维度对比 |
| 栈 vs 队列 | 操作特性对比 |
| 各种树 | 二叉树/BST/AVL/红黑/B树/B+树 对比 |
| **排序算法总表** ★ | 时间（最好/平均/最坏）、空间、稳定性、是否原地 |
| 查找算法对比 | 顺序/折半/分块/哈希 的 ASL 与适用条件 |
| 图的存储 | 邻接矩阵 vs 邻接表 |

#### 4. 复杂度速查
常见复杂度阶的对比（O(1) < O(logn) < O(n) < O(nlogn) < O(n²) < O(2ⁿ)），
配增长曲线图。

---

## 五、前端呈现要求

### 布局
```
┌──────────────┬────────────────────────────┐
│              │  面包屑：C语言 > 指针 > 指针运算  │
│  章节树      ├────────────────────────────┤
│  （可折叠    │                            │
│   可搜索）   │  ## 指针的算术运算            │
│              │                            │
│  ▾ C语言     │  正文内容...                │
│    ▾ 指针    │                            │
│      · 指针  │  ┌─ 示例（可运行）────────┐  │
│        运算  │  │ 代码编辑器              │  │
│      · 指针  │  │ [运行] [对照预期输出]    │  │
│        与数组│  └────────────────────────┘  │
│  ▾ 数据结构  │                            │
│              │  💡 要点    ⚠️ 易错点        │
│              │                            │
│              │  🎬 相关演示   📝 相关题目    │
└──────────────┴────────────────────────────┘
```

### 必备功能
1. **三级导航树**：C语言 / 数据结构 → 章 → 节
2. **全局搜索**：支持标题、正文、代码全文检索（建议接入轻量搜索库如 MiniSearch，纯前端可跑）
3. **代码一键运行**：示例代码块内嵌运行按钮，调用 Piston
4. **代码块复制**
5. **学习进度标记**：已读/未读，存 localStorage
6. **关联跳转**：知识点 ↔ 演示 ↔ 题目 三向跳转
7. **打印/PDF 导出**（可选，用 `@media print`）
8. **深色模式适配**

### 视觉
- 卡片式布局，要点用图标 + 色块区分（💡 要点 / ⚠️ 易错 / 🎬 演示 / 📝 练习）
- 对比表格用斑马纹 + 吸顶表头
- 复杂度用彩色徽章（O(1) 绿 / O(logn) 蓝 / O(n) 黄 / O(n²) 橙 / O(2ⁿ) 红）

---

## 六、数据文件组织

```
src/data/knowledge/
├── index.json              # 全部知识点的索引（id, title, category, chapter, order）
├── c/
│   ├── ch01_basics.json
│   ├── ch03_data.json
│   ├── ch05_operator.json
│   ├── ch09_pointer.json     ← 最大，可能需要拆成多文件
│   └── ...
├── ds/
│   ├── ch01_intro.json
│   ├── ch02_linear.json
│   └── ...
└── reference/              # 速查表
    ├── operators.json      # 运算符优先级表
    ├── keywords.json
    ├── printf_format.json
    └── sort_compare.json   # 排序算法总表
```

**加载策略**：`index.json` 全量加载（仅元数据，几 KB），
点击某章时再 `fetch` 对应的章节文件。

---

## 七、编写要求

1. **语言通俗**：面向初学者，避免堆砌术语。每引入一个术语先给直觉解释
2. **代码可运行**：示例代码必须完整、可编译，且经过验证
3. **要点不贪多**：每张卡片 3-6 个要点，超过说明该拆分
4. **易错点必须写**：这是知识库区别于教科书的关键价值
5. **相互引用**：指针章节要关联到链表演示，链表题目要回指指针知识点
6. **不照抄教材**：用自己的话重新组织，可参考大纲结构
