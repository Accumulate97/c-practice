# 数据结构可视化演示规范

> C practice 不只是刷题站，**数据结构的概念模型要能"动起来"**。
> 本文件定义需要实现哪些演示、每类演示的交互方式与技术方案。

---

## 一、技术选型建议

**推荐：SVG + React 状态驱动**

| 方案 | 评价 |
|---|---|
| **SVG + React** ✅ | 元素可被 CSS/JS 直接操作、支持过渡动画、可缩放不失真、易调试 |
| Canvas | 性能好但需全量重绘，调试困难，不适合教学演示 |
| D3.js | 功能强但体积大（~90KB），本场景用不到其数据绑定能力 |

**动画方式**：React state 保存"当前算法步骤快照"，
渲染层根据快照绘制 SVG，配合 CSS `transition` 实现平滑位移。

**核心抽象**：
```typescript
// 算法以"生成器"方式产出每一步快照
type Step = {
  snapshot: VisualState;   // 当前帧的完整状态
  description: string;     // 这一步的解说文字
  highlight?: string[];    // 需要高亮的元素 id
};

function* bubbleSort(a: number[]): Generator<Step> {
  // ... 每次 yield 一个 Step
}
```

**好处**：算法逻辑与渲染解耦，同一套 Step 可驱动"自动播放/单步/后退"。

---

## 二、必须实现的演示清单

### A. 线性表（优先级 ★★★★★）

| 演示 | 内容 | 交互 |
|---|---|---|
| 单链表 | 结点 + 指针箭头 | 插入/删除/查找，支持指定位置 |
| 双向链表 | 前驱/后继双箭头 | 插入/删除，展示 4 个指针的修改顺序 |
| 循环链表 | 尾结点指回头 | 与普通链表对比 |
| 顺序表 vs 链表 | 并排展示 | 同一操作（如中间插入）的对比动画 |

**关键教学点**：插入/删除时**指针修改顺序**（先连后断），这是学生最易错处。

### B. 栈与队列（★★★★★）

| 演示 | 内容 | 交互 |
|---|---|---|
| 顺序栈 | 数组 + top 指针 | push/pop，展示栈满/栈空 |
| 链栈 | 链表头插 | push/pop |
| 循环队列 | 环形数组 + front/rear | 入队/出队，**重点展示队空与队满的判别** |
| 括号匹配 | 栈的应用 | 逐字符扫描，展示入栈出栈 |
| 表达式求值 | 中缀转后缀 + 计算 | 两个栈联动 |

**关键教学点**：循环队列中 `front == rear` 的歧义（队空 vs 队满），
三种解决方案（少用一个空间 / 设标志位 / 计数值）应有对比演示。

### C. 树（★★★★★）

| 演示 | 内容 | 交互 |
|---|---|---|
| 二叉树遍历 | 前序/中序/后序/层序 | 单步高亮访问路径，展示递归栈 |
| 二叉树构建 | 由遍历序列还原树 | 输入序列生成树 |
| 二叉排序树 | 插入/删除/查找 | 展示查找路径与树的形态变化 |
| 平衡二叉树 | LL/RR/LR/RL 旋转 | **旋转前后对比动画**（重点） |
| 哈夫曼树 | 构造过程 | 每次选最小两结点合并 |
| 线索二叉树 | 线索化 | 展示空指针改为线索 |

**关键教学点**：AVL 的四种旋转是难点，动画必须能**逐步拆解**。

### D. 图（★★★★）

| 演示 | 内容 | 交互 |
|---|---|---|
| 邻接矩阵 / 邻接表 | 两种存储的对照 | 输入图后并排显示 |
| DFS | 深度优先遍历 | 递归栈可视化 + 访问顺序 |
| BFS | 广度优先遍历 | 队列可视化 + 层次扩散 |
| 最小生成树 | Prim / Kruskal | 逐边加入，Prim 展示点集扩张、Kruskal 展示并查集合并 |
| 最短路径 | Dijkstra / Floyd | 距离表实时更新，高亮松弛操作 |
| 拓扑排序 | 入度表 + 队列 | 逐点删除，检测环 |
| 关键路径 | AOE 网 | 最早/最晚发生时间，高亮关键活动 |

### E. 排序（★★★★★）

**必须实现**（8 种）：
冒泡、选择、插入、希尔、归并、快速、堆、基数

**每种的演示要素**：
- 柱状图表示数组（高度 = 数值）
- 颜色语义：**比较中**（黄）、**交换**（红）、**已就位**（绿）、**基准元**（蓝）
- 统计面板：比较次数、交换次数、移动次数
- 控制：播放/暂停、单步、后退、速度调节（0.5x~4x）、重置
- 随机数据生成 / 手动输入

**对比模式**：选中多种算法，用同一组数据同步跑，横向对比步数与耗时。
这是排序算法最有说服力的教学形式。

### F. 查找（★★★）

| 演示 | 内容 |
|---|---|
| 顺序查找 | 逐个比对 |
| 折半查找 | 区间收缩动画，展示 low/mid/high |
| 二叉排序树查找 | 路径高亮 |
| 散列表 | 哈希函数计算 → 桶定位 → 冲突处理（拉链法/开放地址） |

### G. 串（★★★）
- KMP 模式匹配：主串指针不回溯的动画，next 数组的推导过程
- BF 与 KMP 对比

### H. 内存与指针（C 语言特有，★★★★★）

**这一块对 C 语言学习者极有价值，务必实现**：

| 演示 | 内容 |
|---|---|
| 指针与数组 | `p++` 时指针在内存格上移动 |
| 多级指针 | `int **pp` 的指向关系图 |
| 函数调用栈 | 调用时的栈帧压入/弹出，形参实参关系 |
| 值传递 vs 地址传递 | 并排动画，直观展示为何 swap 需传地址 |
| malloc/free | 堆内存的申请与释放，野指针/内存泄漏的产生 |

---

## 三、交互规范（所有演示统一）

### 必备控件
1. **播放 / 暂停**
2. **单步前进 / 单步后退**（后退靠保存的快照数组实现，不要重新逆运算）
3. **速度调节** 0.5x / 1x / 2x / 4x
4. **重置**
5. **数据输入**：随机生成 + 手动输入 + 预设样例（如"逆序数组""已排序数组"）
6. **步骤解说区**：显示当前步在做什么（对应 Step.description）
7. **伪代码/代码同步高亮**：右侧显示算法代码，高亮当前执行的行

### 视觉规范
- 颜色语义统一（见上）
- 动画时长 300-500ms，过快看不清、过慢烦躁
- 高亮元素加粗边框 + 轻微缩放
- 深色/浅色主题都要适配

---

## 四、目录结构建议

```
src/
├── viz/                      # 可视化模块
│   ├── core/
│   │   ├── Step.ts           # Step 类型定义
│   │   ├── Player.tsx        # 播放器组件（播放/单步/速度）
│   │   └── Canvas.tsx        # SVG 画布容器
│   ├── linear/               # 线性表
│   │   ├── SinglyLinkedList.tsx
│   │   ├── DoublyLinkedList.tsx
│   │   └── ...
│   ├── stack/
│   ├── tree/
│   ├── graph/
│   ├── sort/                 # 排序算法
│   │   ├── algorithms/       # 纯算法，产出 Step 生成器
│   │   │   ├── bubbleSort.ts
│   │   │   └── ...
│   │   └── SortBoard.tsx     # 通用排序可视化面板
│   └── memory/               # 指针与内存（C 语言特有）
├── data/
│   ├── problems/             # 题目 JSON
│   └── knowledge/            # 知识点 JSON
└── pages/
```

**关键**：算法文件（`algorithms/*.ts`）必须是**纯函数/生成器**，
不依赖 React 与 DOM，便于单测与复用。

---

## 五、性能注意事项

- 单步快照数组：数据量控制在 **n ≤ 50**，超过时提示（否则快照数爆炸）
- 排序演示步数：冒泡 50 元素约 1200 步，需按需生成而非一次性算完
- 使用 `requestAnimationFrame` 驱动播放，避免 `setInterval` 丢帧
- 图/树的 SVG 节点数控制在 100 以内

---

## 六、验收标准

每个演示完成后自查：

- [ ] 能否单步执行并后退？
- [ ] 当前步骤是否有文字解说？
- [ ] 关键状态变化是否有高亮？
- [ ] 能否自定义数据？
- [ ] 深色模式下是否清晰？
- [ ] 移动端/窄屏是否可用（至少不破版）？
- [ ] 算法逻辑是否与教材一致（命名、边界处理）？
