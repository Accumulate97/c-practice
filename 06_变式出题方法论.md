# 变式出题方法论

> 本文件说明**如何把教材内容转化（变式）为练习题**。
> 这是 C practice 题库的主要生产方式。

---

## 一、为什么用变式而非原创

| 方式 | 优点 | 缺点 |
|---|---|---|
| 完全原创 | 无版权风险 | 难度、考点覆盖难以把控，容易偏离教学重点 |
| 照抄原题 | 质量有保证 | **侵权**，且网上能搜到答案，练习价值低 |
| **变式改编** ✅ | 保留经典考点的命题质量；形成新题；规避版权 | 需要方法论 |

**核心原则**：借鉴**考点与命题角度**，改造**数据、场景、问法**，产出**新题**。

---

## 二、七种变式手法

### 手法 1：换数据（最基础）
保留题目结构与考点，更换具体数值。

```
原题：求 1+2+...+100 的和
变式：求 1+3+5+...+99 的奇数和

考点不变（循环累加），但需要重新思考步长与边界。
```

> ⚠️ 单纯换数字不算有效变式。必须让数据变化带来**新的思考点**，
> 例如上例从"步长 1"变成"步长 2"，边界条件随之改变。

### 手法 2：换场景（推荐）
保留算法内核，更换应用场景与表述。

```
考点：数组逆置
原场景：将数组 a 中 n 个元素逆序存放
变式：将一句话按单词逆序输出（"I love C" → "C love I"）
      将十进制整数转为二进制（除2取余，结果需逆序输出）

考点都是"逆序"，但包装不同，学生需要迁移。
```

### 手法 3：换问法（升维）
同一段代码，从不同角度提问。

```
给定一段冒泡排序代码：
 - 问输出        → code_reading
 - 问交换次数    → 需要手动模拟
 - 挖空循环条件  → code_completion
 - 改成升序      → 需要理解比较符号
 - 分析复杂度    → complexity
 - 植入错误      → debug
```

**这是最高效的变式手法**：一份素材产出 6 道题，覆盖 6 种题型。

### 手法 4：合并
把两个独立知识点合并到一道题里。

```
知识点 A：字符串遍历
知识点 B：字符统计
合并题：统计字符串中数字字符、字母字符、其他字符的个数

难度从 2 升到 3，考察知识组合能力。
```

### 手法 5：降维 / 升维
- **降维**：把复杂算法拆成若干小步，每步单独出题（用于基础题）
- **升维**：把简单操作组合成完整算法（用于提高题）

```
降维：快速排序 → ① 写出 partition 函数 ② 写出递归框架 ③ 合并
升维：数组查找 + 计数 + 排序 → "统计并输出出现次数最多的元素"
```

### 手法 6：错误植入（生成改错题）
**反向操作**：先写正确代码，再按"初学者 bug 库"植入 2-3 个错误。

植入时必须保证：
- 错误是**真实初学者会犯的**，不是刻意刁难
- 修正后代码**必须能通过测试用例**
- 错误分布在不同行

### 手法 7：条件反转
把题目的已知与求解互换，或反转条件。

```
原题：输入 n，判断是否为素数
反转：输入 n，输出 n 以内的所有素数
再反转：输入 n，输出 n 以内孪生素数对（相差 2 的素数对）
```

---

## 三、从教材章节到题目的转化流程

```
1. 读大纲条目（01 / 02 号文件）
   ↓ 确定本章知识点清单
2. 读教材原题（03 号文件，若有）
   ↓ 理解该知识点的经典考法
3. 选题型（按 04 号文件的题型配比）
   ↓
4. 应用变式手法生成新题
   ↓
5. 检查：是否重复？是否超纲？难度是否合适？
   ↓
6. 写代码 → Piston 实机验证
   ↓
7. 写 explanation 详解
```

---

## 四、各题型的变式优先级

| 目标题型 | 最有效的变式手法 |
|---|---|
| 程序填空 | 手法 3（换问法·挖空）+ 手法 5（降维） |
| 程序改错 | 手法 6（错误植入） |
| 程序阅读 | 手法 1（换数据）+ 手法 3（换问法） |
| 编程题 | 手法 2（换场景）+ 手法 4（合并）+ 手法 7（反转） |
| 概念选择 | 直接依据大纲知识点编写，参考 03 的干扰项设计 |

---

## 五、质量自检清单

每道题生成后逐项检查：

- [ ] 考点是否在 `01`/`02` 大纲范围内？
- [ ] 代码是否通过 Piston 实机验证？
- [ ] `explanation` 是否讲清了"为什么"？
- [ ] 与本章其他题是否重复（不能只换变量名）？
- [ ] 难度标注是否准确（1-5）？
- [ ] Bloom 层级是否过高集中在 remember？
- [ ] 干扰项/错误是否来自真实误解？
- [ ] 若为变式，`source` 是否注明了出处？
- [ ] 测试用例是否覆盖边界？

---

## 六、关于 source 字段的写法

| 情况 | 写法 |
|---|---|
| 教材原题直录 | `"source": "《C程序设计试题汇编》题1.1"` |
| 依据原题改编 | `"source": "变式（源自《C程序设计试题汇编》第5章编程题）"` |
| 依据大纲知识点原创 | `"source": "原创（依据大纲考点：指针算术）"` |
| 依据教材例题改编 | `"source": "改编（源自谭浩强《C程序设计》例8.5）"` |

**必须如实填写**，方便后续人工校对与版权追溯。

---

## 七、⚠️ 版权红线

- ❌ **不得**大段照抄教材题干原文
- ❌ **不得**直接复制教材代码作为题目代码（尤其注意：部分教材 PDF 的 OCR 文本本身有错）
- ❌ **不得**把教材扫描页、PDF 原文提交到公开仓库
- ✅ 借鉴考点、命题角度、题型结构
- ✅ 代码自行编写并经实机验证
- ✅ 项目 README 中注明"题目依据 XX 教材知识点体系编写，由 AI 生成并人工校验中"
