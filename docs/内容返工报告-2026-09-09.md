# 内容返工报告（2026-09-09）

> 会话定位：阶段 4 模块 5 完成（commit cadf987）后的内容返工轮。**不写前端代码**，只做内容修正与补全 +
> 构建期取证脚本增强。覆盖 `docs/待办-backlog.md` 登记的待办 1/2/3，三项全部完成并关闭。
> 全程未触碰 `schema/` 与 `AGENTS.md`。

## 〇、结论速览

| 项 | 结果 |
|---|---|
| 待办 1 | 48/48 道 programming reference 补齐，实机全数通过，`verified:true`；expected 零修正 |
| 待办 2 | 39 道阅读题 stdin 补齐并实机复核一致（零 answer 修正）；2 道问「条件」的题按口径退出自动判分；2 处 OCR 笔误修复 |
| 待办 3 | 14 道 debug 题 27 处行号修正，自检 15/15 题 bugs 行号 100% 命中真实差异行；`acceptance:dbg` 37/37 PASS |
| verify:data | 518 题 · **error 0 · warn 142 → 55**（NOT-VERIFIED 140→53，CORPUS-EMPTY 2 不变） |
| judge:verify 全量 | 288 题入队 · **256 PASS / 32 FAIL / 0 未判定**，32 FAIL 全部为已登记残留（R1 片段 28 + R2 环境依赖 4）；最终存档 461 请求（含抖动恢复子集复跑，见 §四） |
| 四型代码题 verified | **256 / 309**，与报告 PASS 数双向核对零差异 |
| build / build:index | 全绿（518 题 / 13 分片） |

## 一、待办 1 —— 48 道 programming 补 reference（完成）

- 复核起点：全库 programming 49 道（`c-ch*.json` 48 道 + `ds-ch02-pg-001`），其中 48 道 `reference` 为空串
  → `buildRunnableSource` 返回 null → 从不进判分队列 → 永远 `verified:false`，`testCases.expected` 从未被实机验证。
- 处理：逐题依据题意 + testCases 编写标准 C 参考实现（完整 main、scanf/printf；
  无 gets/conio.h/getch/system("pause")，无 C++ 语法与类C伪码）。
- 实机复核：全量 `npm run judge:verify`（Godbolt 严格串行，executorRequest+filters.execute 双开关，-O0，软超时 25s），
  **48/48 PASS**，`verified:true` 全部由脚本置位。
- **expected 与实机输出零不符**：没有任何一题需要以实机输出修正 expected，也没有产生 note 记录。
- sqrt/libm：`scripts/judge-verify.ts` 增加构建期 libm 路由——实测 cg132 执行链路不吃 `-lm`
  （undefined reference → didExecute=false），凡判分源码命中 `MATH_LIB_RE` 自动路由 g132，报告新增 `compiler` 字段记录实际所用编译器。
  全库实扫**仅 2 道含 sqrt**：`c-ch07-cr-001`（题 7.18）与 `c-ch08-pg-005`，均 g132 路由 PASS。
  用户点名的 4.73/4.74/7.21/7.29 经全库扫描：判分源码不含 sqrt 调用（或库中无此题号条目）。
  **线上判分后端未动**；该前端缺口登记为残留 R5。

## 二、待办 2 —— code_reading 补 stdin（完成）

- 提取口径：只取题干已写明的输入（□=空格、＜回车＞=\n、全角－=半角-），**不猜测**。
- 填充 **39 道**：首批 38 道 + 本轮收口复扫补捉 1 道——`c-ch04-cr-005`（题 4.59，题干「若运行时输入：2<回车>」）。
  该题此前空 stdin 也能 PASS 属**侥幸**：scanf("%c") 撞 EOF 后未初始化变量恰好走 else 分支；
  本轮补 `stdin:"2\n"` 后 `judge:verify --only=c-ch04-cr-005` 重验 **1/1 PASS**（证据已并入报告）。
- 实机复核：39 道全部与预存 answer **逐字符一致，零 answer 修正、零 note**。
  含 `gets()` 的 8 道在判分队列内于 cg132 编译（仅 warning）并执行通过。
- 顺带修复 2 处 OCR 代码笔误（补字符，不是重写题目）：
  - `c-ch09-cr-001`：`retur(0)` → `return(0)`；
  - `c-ch07-cr-001`：`void fun(int n)` 函数声明补分号 + 补 `#include <stdlib.h>`（消除 implicit declaration）。
- **2 道按用户口径标记为不进自动判分**：`c-ch04-cr-011`（题 4.70）与 `c-ch04-cr-012`（题 4.71）问的是
  「输入值 a 和 b 应满足的条件」，答案是条件描述而非 stdout——无法从题干确定一组输入，不猜测。
  处置（数据层，先例 = `c-ch04-cr-004`）：清空误存输出串的 `answer`、置 `answerIsDescription:true`，
  条件推演写入 `explanation`（4.70：a>0 且 a<b；4.71：a==b 且 a<=0，原输出串在解析中留档）。
  前端 `readingTarget` 对这类题显示「答案是文字描述」面板并禁用判分，行为诚实。

## 三、待办 3 —— 15 道 debug 的 bugs 行号返工（完成）

- 取证：本轮从分片**重新扫描** 15 道题，逐行大小写敏感比对 `code`↔`fixed_code` 得真实差异行，
  与既有逐行取证表（backlog 表格）逐条核对一致后才动手。
- 修正 **27 处**：14 道题的 bugs 开头「第 N 行」26 处 + explanation 复述 1 处
  （`c-ch04-dbg-001`「第 2 处错误是第 9 行」→「第 8 行」）。
- 特殊处理（均已核实）：
  - `c-ch05-dbg-005` b0 正文中的「第 1 行」指**输出行**，不动；只改开头行号（9→7）。
  - `c-ch07-dbg-012` b1 含两处引用：开头 9→8（`p.score += d;`）+ 正文「与第 14 行」→「与第 15 行」（`bump(s, d);`）。
  - `c-ch07-dbg-010` 行号原本全对，零改动（其真实差异行 [2,4,5,6,12] 多于处数属正常：一处逻辑错误牵动多行）。
  - `c-ch06-dbg-008`/`c-ch09-dbg-013`/`c-ch09-dbg-015` 的 bugs 顺序与行号顺序相反（b0 指较大行号），逐条按内容对准，不按顺序硬套。
- 自检：修正后重扫，**15/15 道题每条 bugs 开头行号都命中真实差异行**（修复前仅 1 道全对）。
- 判分回归：`npm run acceptance:dbg` **37/37 PASS**（111 次真实 Godbolt 请求，严格串行，0 传输层复跑；
  unchanged 15 + fixed 15 + partial 1 + partial-scan 5 + syntax 1）。
  `code`/`fixed_code`/`testCases` 一个字节未动 → verified 证据依然有效，按待办 3 口径**未重跑 judge:verify 取证**
  （收尾的全量复跑属流水线例行，结果与修改前一致）。

## 四、全量流水线实测（所有编辑落盘后）

| 命令 | 实测输出（摘要） |
|---|---|
| `npm run judge:verify`（全量·第 1 次） | 题目=288 请求=457 总墙钟≈267s 均值≈584ms/请求；**通过 256/288 · FAIL 32 · 未判定 0**；13 个分片全部「无需改」（verified 标志与报告一致） |
| `npm run judge:verify`（全量·第 2 次，为留一份干净存档） | 撞 Godbolt 排队拥塞：`c-ch03-cr-001~005` 5 道 HTTP 5xx/网络**未判定**（脚本按 AGENTS.md 语义：`verified` 一个字节未动，分片「无需改」），其余 283 道结论与第 1 次一致 |
| `npm run judge:verify --only=c-ch03-cr-001…005`（抖动恢复复跑） | **3/5 PASS**：cr-001/002/003 重新取证通过，cr-004/005 如期 FAIL（R1 无 main 片段）；子集结果按脚本合并语义并入存档 |
| **最终存档 `verification-report.json`** | 题目=288 · 请求=461 · 总墙钟≈369s · 均值≈801ms/请求；`ok=256 / fail=32`；**`inconclusive_this_run=[]`**（复跑已清空）；`last_known_good=256`（成功证据只增不减）；`only` 字段留档最后一次的 5 个 id，属透明元数据，不改变 288 题全量存档口径 |
| `npm run verify:data` | **题目总数 518 · error 0 · warn 55**（NOT-VERIFIED 53 + CORPUS-EMPTY 2） |
| `npm run build:index` | 生成 index.json（518 题/13 分片，内容指纹 2acac8c2）；知识/演示语料 0 属预期。`search/problems.json` 再生成后与 HEAD **字节一致**（搜索索引不含 stdin/reference/verified 字段），故不入本轮 commit |
| `npm run build` | `tsc -b && vite build` ✓ 71 modules · built in 246ms（无类型错误；前端零改动） |
| `npm run verify:pages` | ✅ dist/ 全部资源为相对路径（favicon + 3 js + 1 css），可置任意子目录 |
| `npm run lint:code` | 15 处 banned-gets（存量，见 R4） |

- **warn 演变：142 → 55**（−87）。基线 142 = NOT-VERIFIED 140 + CORPUS-EMPTY 2（docs/verify-report-2026-09-08.md）。
  NOT-VERIFIED 140→53 的 87 道 = 待办 1 的 48 + 待办 2 首批的 38 + 笔误修复的 c-ch09-cr-001。
  （本轮 cr-005 补 stdin 与待办 3 行号修正不改变 warn 计数；cr-011/012 从「判分 FAIL」组转入「空 answer」组，warn 数不变。）
- 期间库量从基线的 493 → 518：内容阶段新增的 15 道 debug + 10 道 code_completion 当时即已实机取证，未贡献 warn。

### 最终 verified 统计

| 口径 | 数值 |
|---|---|
| 全库题量 | 518（single_choice 102 / fill_blank 89 / code_reading 225 / programming 49 / code_completion 20 / debug 15 / short_answer 18） |
| 四型代码题 | 309 |
| 空 answer 不进判分队列 | 21（18 数据缺口 + 3 answerIsDescription） |
| judge 队列 | 288 = 309 − 21 |
| **verified:true（四型）** | **256（82.8%）**，与报告 PASS 双向核对：report ok 但分片未置 = 0；分片置了但不在报告 = 0 |
| FAIL | 32（全部为 R1/R2 登记残留，非本轮引入） |
| last_known_good | 256（成功证据只增不减） |

## 五、失败残留清单（已登记 backlog「残留登记」节，本轮不做）

1. **R1 · 28 道片段式阅读题**（无 main → compile-error，verified:false）：
   c-ch03-cr-004/005/006、c-ch04-cr-019/021/022、c-ch05-cr-004/005/006/007/012/020、
   c-ch06-cr-001/002/004/007/015/016/018/023/024/029、c-ch11-cr-001/002/006/007/011/016。
   救活需补 main 或改按函数判分 = 内容重做，超出「填 stdin」口径。
2. **R2 · 4 道环境依赖阅读题**：c-ch09-cr-051/052（需 argv）、c-ch12-cr-001/002（需磁盘文件/argv）。
   schema 无 argv 字段（加字段 = 改 schema，本轮禁止），stdin 无法表达；其 answer 依教材推演，无法实机取证。
3. **R3 · 21 道空 answer 阅读题**不进判分（前端显示诚实的缺陷面板）；前端写死的「19 道」文案已过时，阶段 5 顺带改。
4. **R4 · lint:code 15 处 banned-gets**（9 道阅读题，教材原文风格）：与 AGENTS.md 禁令冲突，**待用户裁决**是否改写为 fgets。
5. **R5 · libm 前端缺口**：线上 cg132 不吃 -lm，含 sqrt 的 2 道题线上「实机对照」不可用（构建期已 g132 取证）。
6. **R6 · acceptance-dbg.ts 日志「不符」旧文案**：prose 与真实差异行现已一致，纯日志措辞，阶段 5 改。

## 六、本轮改动文件

- `public/data/problems/c-ch03/04/05/06/07/08/09/10/12.json`（9 分片）：reference、stdin、answer/answerIsDescription/explanation、bugs 行号、verified 标志
- `public/data/problems/verification-report.json`：全量复跑存档（compiler 字段、--only 合并语义）
- `public/data/problems/index.json`：build:index 再生成（`public/data/search/problems.json` 同步再生成但字节无差异，未入 commit）
- `scripts/judge-verify.ts`：`--only=` 子集参数（报告合并）+ libm(g132) 构建期路由 + `compiler` 字段（线上后端未动）
- `docs/待办-backlog.md`：待办 1/2/3 关闭戳 + 残留登记 R1~R6
- `docs/内容返工报告-2026-09-09.md`：本报告

未改动：`schema/`、`AGENTS.md`、`src/`（前端零改动）、c-ch01/02/11、ds-ch02 分片。