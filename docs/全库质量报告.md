# 全库质量报告

- 生成时刻：2026/9/14 15:42:07（Asia/Shanghai）
- 生成命令：`node scripts/scan-full-quality.ts`（脚本只读，**不改任何题目数据**）
- 扫描范围：`public/data/problems/` 全部 2074 道题（20 个分片）
- 机器可读全量清单：`tmp/full-quality-scan.json`（已 gitignore，含逐题命中明细与实机 stdout）
- 口径声明：本轮**只报告不修改**。P0/P1 全部进入第九节「待人工复核清单」，由人裁决后再动数据。

## 一、一句话结论

全库 2074 道题结构层命中 123 条（P0 6 / P1 71 / P2 46）；Godbolt 实机复核 99 道「题干自带完整程序」的单选题，其中 52 道真实输出唯一命中 answer 选项。

## 二、规模盘点

| 题型 | 数量 | 占比 |
|---|---:|---:|
| `single_choice` | 644 | 31.1% |
| `code_reading` | 514 | 24.8% |
| `code_completion` | 354 | 17.1% |
| `fill_blank` | 261 | 12.6% |
| `programming` | 162 | 7.8% |
| `debug` | 54 | 2.6% |
| `true_false` | 53 | 2.6% |
| `short_answer` | 23 | 1.1% |
| `complexity` | 9 | 0.4% |
| **合计** | **2074** | 100% |

- `ai_generated: true`：1066 道（51.4%）；既有/教材转录：1008 道
- `verified: true`：2068 道（唯一置位途径是 `npm run judge:verify`，本轮未改动）
- `answerIsDescription: true`：6 道（描述性试题，设计内不进自动判分，故 answer 为空不计缺陷）

## 三、检查项总览

| 严重度 | 检查项 | 命中 | 处置建议 |
|---|---|---:|---|
| P0 | 实机输出唯一匹配的选项不是 answer（`net-answer-mismatch`） | 2 | 答案错，或题干程序与选项设计不符，必须人工裁决 |
| P0 | explanation 与 answer 指向不同选项（`sc-explanation-letter-conflict`） | 4 | 二者必有一错，需人工裁决 |
| P1 | 实机输出不匹配任何选项（`net-no-match`） | 1 | 选项写错/输出格式差异，人工核对 |
| P1 | 题干程序超时（疑似死循环）且无选项对应（`net-abnormal-timeout`） | 1 | 人工确认是否题目本意 |
| P1 | 题干程序运行时崩溃且无选项对应（`net-abnormal-runtime-error`） | 1 | 人工确认是否题目本意 |
| P1 | 真实输出不等于任何具体选项，而「不确定值」选项不是 answer（`net-indeterminate-vs-answer`） | 1 | 多为未初始化变量题，人工裁决 |
| P1 | explanation 少于 50 字（`explanation-short`） | 67 | 补充「为什么」与易错点 |
| P2 | 选项仅空白不同（`sc-option-duplicate-nospace`） | 13 | 输出预测题里换行/空格就是考点，多数属正当设计，仅需人工确认判分口径不会折叠空白 |
| P2 | 元信息里的知情注记（`doubt-marker-meta`） | 33 | 设计内保留，不判失败 |

## 四、选择题答案唯一性（A 结构层）

单选题 644 道、判断题 53 道全量逐题检查：answer 是否合法下标、选项是否重复（原样 / 掩码数字字符串后）、
选项自带字母前缀是否与下标错位、explanation 写的答案字母是否与 answer 字段一致。

### explanation 与 answer 指向不同选项（4 条）

| 题号 | 类型 | 分片 | 明细 |
|---|---|---|---|
| `c-ch05-sc-032` | single_choice | c-ch05.json | explanation 说答案是 D，answer 字段指向 B |
| `c-ch05-sc-035` | single_choice | c-ch05.json | explanation 说答案是 B，answer 字段指向 D |
| `c-ch05-sc-042` | single_choice | c-ch05.json | explanation 说答案是 D，answer 字段指向 B |
| `c-ch05-sc-046` | single_choice | c-ch05.json | explanation 说答案是 C，answer 字段指向 A |

### 选项仅空白不同（13 条）

| 题号 | 类型 | 分片 | 明细 |
|---|---|---|---|
| `c-ch03-sc-012` | single_choice | c-ch03.json | 选项 A 与 C 去掉空白后相同："10A□20B<回车>" |
| `c-ch03-sc-013` | single_choice | c-ch03.json | 选项 C 与 D 去掉空白后相同："scanf(\"%3.2f%2.1f\",&f1,&f2);" |
| `c-ch03-sc-017` | single_choice | c-ch03.json | 选项 A 与 C 去掉空白后相同："scanf(\"%c %c %c \",&ch1,&ch2,&c" |
| `c-ch03-sc-029` | single_choice | c-ch03.json | 选项 A 与 B 去掉空白后相同："/3.14  /003.14/  3.14/" |
| `c-ch05-sc-027` | single_choice | c-ch05.json | 选项 B 与 C 去掉空白后相同："##3\n**4##5" |
| `c-ch05-sc-027` | single_choice | c-ch05.json | 选项 A 与 D 去掉空白后相同："**3##4\n**5" |
| `c-ch05-sc-032` | single_choice | c-ch05.json | 选项 A 与 B 去掉空白后相同："#&\n&\n&*" |
| `c-ch05-sc-032` | single_choice | c-ch05.json | 选项 A 与 C 去掉空白后相同："#\n&\n&\n&\n*" |
| `c-ch05-sc-034` | single_choice | c-ch05.json | 选项 A 与 C 去掉空白后相同："m%5==0" |
| `c-ch06-sc-044` | single_choice | c-ch06.json | 选项 A 与 B 去掉空白后相同："aa\nbb\ncc\ndd" |
| `c-ch06-sc-044` | single_choice | c-ch06.json | 选项 A 与 D 去掉空白后相同："aa bb\ncc\ndd" |
| `c-ch06-sc-045` | single_choice | c-ch06.json | 选项 A 与 B 去掉空白后相同："a\nb\nc\nd\ne\nf" |
| `c-ch09-sc-018` | single_choice | c-ch09.json | 选项 A 与 B 去掉空白后相同："\"abcd \"" |


## 五、选择题答案唯一性（B 实机层，Godbolt 真跑）

结构层查不出「选项写得像但程序真实输出是另一个」这类问题，所以把 99 道「stem 自带完整可编译程序、选项像输出候选」的单选题
送去 Godbolt 实机跑（编译参数与线上判分同源：`-std=c99 -Wall -Wextra`，严格串行、尊重 `minIntervalMs`），
把真实 stdout 与 4 个选项逐一比对。选项里的 `□` / `·` 是「一个空格」的印刷记号，比对时还原成空格但不折叠其它空白。

| 判定 | 数量 | 含义 |
|---|---:|---|
| `ok-unique` | 52 | 真实输出逐字等于、且只等于 answer 指向的选项 |
| `excluded-fragment` | 17 | 题干代码不是完整可编译程序（考表达式/改错），实机层不适用 |
| `ok-modulo-whitespace` | 10 | 真实输出与 answer 选项仅差空白（□ 记号/缩进书写差异） |
| `nondeterministic-output` | 5 |  |
| `excluded-needs-input` | 5 |  |
| `ok-embedded` | 2 | 选项是描述句，真实输出唯一嵌在 answer 指向的那句里 |
| `answer-mismatch` | 2 | 真实输出唯一匹配的选项不是 answer → 答案错 |
| `ok-indeterminate` | 1 | 输出确实不可预测，而 answer 正是「不确定值」 |
| `abnormal-runtime-error` | 1 | 程序运行时崩溃且无选项对应 |
| `abnormal-timeout` | 1 | 程序超时且无选项对应 |
| `indeterminate-vs-answer` | 1 | 输出不可预测，但 answer 指向某个具体值 |
| `ok-abnormal-is-answer` | 1 | 程序确实崩溃/超时，而 answer 正是对应选项 |
| `no-match` | 1 | 真实输出不匹配任何选项 |
| **合计** | **99** | |

其中 17 道的题干代码不是完整可编译程序（这类题考「哪个表达式对」「哪句 scanf 对」，不是输出预测），实机层不适用，单列为 excluded-fragment（不计缺陷）。

### 需人工复核的 16 题

| 题号 | 判定 | answer | 命中 | 真实 stdout（前 50 字） | 说明 |
|---|---|---|---|---|---|
| `c-ch03-sc-015` | nondeterministic-output | B | — | "���" | 真实输出含控制字符/替换符（"���"），是未初始化或越界读的结果，不能当证据判答案错 |
| `c-ch04-sc-028` | excluded-needs-input | A | — | "302155874" | 题干程序读 stdin，而选择题没有结构化 stdin（输入常写在题干散文里），实机输出不可作为证据 |
| `c-ch04-sc-029` | excluded-needs-input | D | — | "Enter x:x=-793565816:f(x)=0" | 题干程序读 stdin，而选择题没有结构化 stdin（输入常写在题干散文里），实机输出不可作为证据 |
| `c-ch05-sc-011` | abnormal-runtime-error | A | — | "\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001\u0001" | 题干程序实机运行时崩溃，且没有任何选项对应该事实 |
| `c-ch05-sc-019` | abnormal-timeout | D | — | "" | 题干程序实机超时（疑似死循环），且没有任何选项对应该事实 |
| `c-ch05-sc-042` | excluded-needs-input | B | — | "0.000000" | 题干程序读 stdin，而选择题没有结构化 stdin（输入常写在题干散文里），实机输出不可作为证据 |
| `c-ch06-sc-044` | nondeterministic-output | A | — | "��\n\nDΚ�\n�x" | 真实输出含控制字符/替换符（"��\n\nDΚ�\n�x"），是未初始化或越界读的结果，不能当证据判答案错 |
| `c-ch06-sc-045` | nondeterministic-output | D | — | "������" | 真实输出含控制字符/替换符（"������"），是未初始化或越界读的结果，不能当证据判答案错 |
| `c-ch06-sc-046` | nondeterministic-output | A | — | "\u0006" | 真实输出含控制字符/替换符（"\u0006"），是未初始化或越界读的结果，不能当证据判答案错 |
| `c-ch09-sc-019` | excluded-needs-input | B | — | "0" | 题干程序读 stdin，而选择题没有结构化 stdin（输入常写在题干散文里），实机输出不可作为证据 |
| `c-ch09-sc-023` | indeterminate-vs-answer | B | D | "-32" | 真实输出 "-32" 不等于任何具体选项，而选项 D 是「不确定值」类，answer=B |
| `c-ch09-sc-024` | nondeterministic-output | C | — | "\u0006" | 真实输出含控制字符/替换符（"\u0006"），是未初始化或越界读的结果，不能当证据判答案错 |
| `c-ch09-sc-047` | no-match | B | — | "ce9f0f90\nce9f0fb4" | 真实输出 "ce9f0f90\nce9f0fb4" 不匹配任何选项 |
| `c-ch10-sc-011` | answer-mismatch | A | D | "12" | 真实输出逐字等于选项 D，但 answer=A |
| `c-ch10-sc-038` | answer-mismatch | B | D | "8" | 真实输出逐字等于选项 D，但 answer=B |
| `c-ch12-sc-015` | excluded-needs-input | C | — | "Enter the infile name :\nEnter the outfile name :\nC" | 题干程序读 stdin，而选择题没有结构化 stdin（输入常写在题干散文里），实机输出不可作为证据 |

> 「命中」列：字母 = 严格等值命中（判分口径）；字母后带 ~ = 仅折叠空白后命中。

## 六、explanation 质量（C）

- 缺失 / 为空：**0** 道（AGENTS.md 第三节要求每道题必须带 explanation）
- 少于 50 字（去 markdown 记号后按字符计）：**67** 道

| 题号 | 类型 | 分片 | 明细 |
|---|---|---|---|
| `c-ch05-sc-010` | single_choice | c-ch05.json | 47 字："答案C。num 从 0 起，条件 `num<=2` 成立三次；循环体内先自增再输" |
| `c-ch05-sc-022` | single_choice | c-ch05.json | 35 字："答案B。省略表达式 2 表示条件恒真，等价于 `for(表达式1; 1; 表达式" |
| `c-ch05-sc-023` | single_choice | c-ch05.json | 38 字："答案B。初值 i=2，条件 `i==0` 一开始就为假，循环体一次也不执行，执行" |
| `c-ch05-sc-024` | single_choice | c-ch05.json | 46 字："答案C。`(y=123)` 赋值后恒为真，条件真假只由 `x<4` 决定；x 取" |
| `c-ch05-sc-029` | single_choice | c-ch05.json | 48 字："答案C。`i++<4` 先比较后自增：1<4、2<4、3<4 三次为真，第四次 " |
| `c-ch05-sc-040` | single_choice | c-ch05.json | 45 字："答案B。把偶数 a 拆成两个素数 b 与 a-b：内层分别验证 b 与 a-b " |
| `c-ch05-sc-045` | single_choice | c-ch05.json | 48 字："答案A。i 为奇数时输出 `*` 后继续输出 `#`；i 为偶数时 contin" |
| `c-ch06-sc-001` | single_choice | c-ch06.json | 42 字："答案D。数组下标只能是整型常量或整型表达式，\"任何类型的表达式\"（如浮点、指针）" |
| `c-ch06-sc-003` | single_choice | c-ch06.json | 41 字："答案D。a[10-10] 即 a[0] 合法；a[10] 越界、a[3.5] 下" |
| `c-ch06-sc-004` | single_choice | c-ch06.json | 28 字："答案A。一维数组定义形式为：类型说明符 数组名[常量表达式]。" |
| `c-ch06-sc-006` | single_choice | c-ch06.json | 36 字："答案C。二维数组可省第一维不可省第二维；A 省第二维，B、D 用圆括号非法。" |
| `c-ch06-sc-007` | single_choice | c-ch06.json | 45 字："答案C。a[1+1][0] 下标为整型表达式且未越界；a[3][4] 越界、a[" |
| `c-ch06-sc-008` | single_choice | c-ch06.json | 30 字："答案D。int a[3][4] 列下标范围 0~3，a[0][4] 越界属非法引" |
| `c-ch06-sc-009` | single_choice | c-ch06.json | 47 字："答案B。int a[][3] 由两行初值推定第一维；A 省第二维、C 花括号不配" |
| `c-ch06-sc-010` | single_choice | c-ch06.json | 30 字："答案C。a[2][3] 只允许 2 行初值，给出 3 行越界，不能正确初始化。" |
| `c-ch06-sc-011` | single_choice | c-ch06.json | 21 字："答案D。{0} 会把数组全部元素初始化为 0。" |
| `c-ch06-sc-012` | single_choice | c-ch06.json | 36 字："答案D。未显式给出的元素也自动补 0，故\"其余元素得不到初值\"的说法错误。" |
| `c-ch06-sc-015` | single_choice | c-ch06.json | 32 字："答案C。float 数组却用 %d 读入，第 3 行格式符与变量类型不符。" |
| `c-ch06-sc-016` | single_choice | c-ch06.json | 39 字："答案D。scanf(\"%d\",&a) 的 &a 是整个数组的地址而非元素地址，第" |
| `c-ch06-sc-017` | single_choice | c-ch06.json | 23 字："答案D。第 3 行循环上界 i<4 会访问 a[3]，越界。" |
| `c-ch06-sc-018` | single_choice | c-ch06.json | 38 字："答案D。a[0][0] 居第 1 位时，a[i][j] 之前有 i 整行加 j " |
| `c-ch06-sc-019` | single_choice | c-ch06.json | 28 字："答案B。5 个初值依次赋给 a[0]~a[4]，其余补 0，语句合法。" |
| `c-ch06-sc-020` | single_choice | c-ch06.json | 26 字："答案B。y[5] 只有 5 个元素却给了 6 个初值，定义不正确。" |
| `c-ch06-sc-021` | single_choice | c-ch06.json | 25 字："答案B。7 个初值按每行 3 个排布需要 3 行，末行补 0。" |
| `c-ch06-sc-022` | single_choice | c-ch06.json | 27 字："答案B。数组名 x 是地址常量，不能赋给 float 变量 y。" |
| `c-ch06-sc-023` | single_choice | c-ch06.json | 33 字："答案A。k=0,1,2 依次取 a[0][2]、a[1][1]、a[2][0]，" |
| `c-ch06-sc-024` | single_choice | c-ch06.json | 38 字："答案B。while(j>=0 && t>a[j]) 把比 t 小的元素后移再插入" |
| `c-ch06-sc-025` | single_choice | c-ch06.json | 27 字："答案D。a[3][3] 共 9 个元素，\"包含 7 个元素\"的说法错误。" |
| `c-ch06-sc-026` | single_choice | c-ch06.json | 32 字："答案C。(i/j)*(j/i) 仅当 i==j 时为 1，其余为 0，输出单位矩" |
| `c-ch06-sc-027` | single_choice | c-ch06.json | 42 字："答案C。代入 i=1..5：9*(-1)%5=-4、0、9%5=4、54%5=4" |
| `c-ch06-sc-028` | single_choice | c-ch06.json | 30 字："答案D。\"abcde\" 加结束符共 6 字节，char s[5] 放不下。" |
| `c-ch06-sc-029` | single_choice | c-ch06.json | 24 字："答案B。%s 遇第一个 '\\0' 即停止，只输出 ab。" |
| `c-ch06-sc-030` | single_choice | c-ch06.json | 24 字："答案D。a 含结束符长 7，b 长 6，a 的长度比 b 长。" |
| `c-ch06-sc-031` | single_choice | c-ch06.json | 42 字："答案B。scanf(\"%s%s\",a,b) 以数组名为地址合法；gets 一次只" |
| `c-ch06-sc-032` | single_choice | c-ch06.json | 36 字："答案D。puts 一次只输出一个字符串，需 puts(a),puts(b) 两次" |
| `c-ch06-sc-033` | single_choice | c-ch06.json | 30 字："答案D。strcpy 只覆盖前 4 个字节，a[5] 仍是原来的 'f'。" |
| `c-ch06-sc-034` | single_choice | c-ch06.json | 23 字："答案D。数组名是地址常量，a=b 不能通过编译。" |
| `c-ch06-sc-035` | single_choice | c-ch06.json | 37 字："答案B。strlen 计到第一个 '\\0' 为止，前面只有 \\t \\v \\\\ 三" |
| `c-ch06-sc-036` | single_choice | c-ch06.json | 30 字："答案D。字符串比较必须用 strcmp，== 比较的是两个地址。" |
| `c-ch06-sc-037` | single_choice | c-ch06.json | 27 字："答案D。s1 大于 s2 当且仅当 strcmp(s1,s2)>0。" |

与 answer 矛盾的自检结果见第四、五节（`sc-explanation-letter-conflict` / `tf-explanation-conflict` / `net-answer-mismatch`）。

## 七、存疑残留标记（D）

正则：`\[\?\]|存疑|待裁决|待确认|待定|待补|TODO|FIXME|TBD|XXX|PENDING-REAL-STDOUT|占位|\?\?\?`（比 `npm run scan:doubt` 更宽，多扫 待定/待补/TBD/XXX/占位/???）。

- 学生可见的表面文本（stem/options/blanks/answer/solution/code/testCases）：**0** 处
- 元信息（explanation/source 等知情注记，设计内保留）：**34** 处

## 八、字段自洽（E）与 judge:verify 复跑

**0 命中**：id 无重复、代码题都能取到可运行源码与 testCases、填空下划线数与 blanks 条数一致、元字段取值合法。

### judge:verify 全量复跑（数据改动后的漂移检查）

- 报告：`public/data/problems/verification-report.json`，生成于 2026-09-14T06:33:10.134Z
- 覆盖代码题 **1078** 道，实机请求 **2157** 次，通过 **1078/1078**
- 后端抖动导致「未判定」：**0** 道；沿用 last_known_good 证据：**0** 道
- 复跑命令：`node scripts/judge-verify.ts --no-write`（只跑不改数据；`verified` 一个字节都没动）

## 九、待人工复核清单

P0 6 条 + P1 71 条，按检查项分组（完整明细见 `tmp/full-quality-scan.json`）：

| 优先级 | 检查项 | 条数 | 题号（前 12） |
|---|---|---:|---|
| P0 | 实机输出唯一匹配的选项不是 answer | 2 | `c-ch10-sc-011` `c-ch10-sc-038` |
| P0 | explanation 与 answer 指向不同选项 | 4 | `c-ch05-sc-032` `c-ch05-sc-035` `c-ch05-sc-042` `c-ch05-sc-046` |
| P1 | 实机输出不匹配任何选项 | 1 | `c-ch09-sc-047` |
| P1 | 题干程序超时（疑似死循环）且无选项对应 | 1 | `c-ch05-sc-019` |
| P1 | 题干程序运行时崩溃且无选项对应 | 1 | `c-ch05-sc-011` |
| P1 | 真实输出不等于任何具体选项，而「不确定值」选项不是 answer | 1 | `c-ch09-sc-023` |
| P1 | explanation 少于 50 字 | 67 | `c-ch05-sc-010` `c-ch05-sc-022` `c-ch05-sc-023` `c-ch05-sc-024` `c-ch05-sc-029` `c-ch05-sc-040` `c-ch05-sc-045` `c-ch06-sc-001` `c-ch06-sc-003` `c-ch06-sc-004` `c-ch06-sc-006` `c-ch06-sc-007` … |

## 十、结论与建议

1. **答案唯一性**：结构层 17 条命中、实机层 33 条异常。
   实机层是本轮新增的证据来源——它把「AI 生成的选项是否与真实程序行为一致」从主观判断变成了可复跑的事实。
2. **explanation**：全库无缺失即为达标底线；短解析清单已列出，建议按章节批量补「为什么 + 易错点」，不必逐条重写。
3. **不自动改数据**：本轮严格遵守「只报告不修改」。P0 条目建议按题型分批裁决，改完必须重跑 `npm run judge:verify` 与 `npm run verify:data`。
4. **复跑方式**：`node scripts/scan-full-quality.ts`（含实机，约 2 分钟）；`--no-net` 只做本地结构检查（约 1 秒），适合接进 CI。
