# 进度流水（大会话 1 · 阶段 10 收口）

每完成一个子任务追加一行。格式：`日期 | 子任务 | 产出 | 验收`。

- 2026-09-11 | 阶段 10-1 三向联动闭环 | 新增 `src/modules/knowledge/`（loader + links）、`KnowledgeListPage` / `KnowledgeDetailPage`、`Markdown` / `CodeBlock` 组件、`scripts/acceptance-stage10.mjs`；`build-index.ts` 知识索引补 file/category/order/summary/keyPoints；删除死配置 `SectionPage`；题目页与演示页新增反向关联区 | `npm run acceptance:stage10` 30/30 全绿（列表页筛选/搜索/分页、卡片→演示→题目→卡片闭环、404 与索引 500 重试、深浅色、375px，控制台 0 error 0 warn）；typecheck / verify:pages 通过
- 2026-09-11 | 阶段 10-2 GitHub Pages 部署配置 | 新增 `.github/workflows/deploy.yml`（push main → npm ci → typecheck → build → verify:pages → verify:data → `.nojekyll` → actions/deploy-pages；UI 验收独立 job 且不阻塞发布）、`scripts/acceptance-subpath.mjs`（node:http 把 dist 挂在 `/C-practice/` 下用真实 Chrome 验子目录形态）、npm scripts 补 `acceptance:stage10` / `acceptance:subpath` | base 已是 `./` + createHashRouter，无需 SPA 404 兜底；`node scripts/acceptance-subpath.mjs` 9/9 全绿（首页 200、无绝对资源引用、三板块数据可拉、三条深链刷新不 404、控制台 0 error）；verify:pages 通过；按要求未 push
- 2026-09-11 | 阶段 10-3 勘误表 · 搜索接线 · 空状态与降级 | 新增 `docs/errata.md`（原书印刷缺陷 10 题 + 缺项/工程缺陷类）并由 `build-index.ts` 拷至 `public/data/errata.md`、`src/pages/ErrataPage.tsx`（fetch + Markdown 渲染 + 失败重试）、页脚勘误入口、`Markdown.tsx` 行内链接支持（题号可点）；`ProblemListPage.tsx` 接入前端搜索（`[data-role="search-input"]`，空格分词 AND，命中数与索引现算一致）；确认 Piston / Wandbox 为 ADR-0001 已否决的死代码，降级链路只验「诚实未判定」 | `npm run acceptance:stage10` 43/43 全绿（新增 errata 6 条 / search 5 条 / fallback 4 条：Godbolt 503 → 「本次未判定」而非白屏、明说不计正确率不置 verified、绝不伪造通过、控制台 0 error 0 warn）；typecheck / verify:pages 通过
- 2026-09-11 | 阶段 10-4 响应式 · 无障碍 · 性能打磨 | `styles/global.css` 新增文字级语义令牌（--fg-ok/-bad/-warn/-link，深浅各一套）+ 链接色统一 + 滑块自绘（可点区 24px、轨道 8px）；65 处正文文字色从图形令牌改走文字令牌（`src/modules/viz/` 图形语义色不动）、6 处品牌蓝改 --fg-link；`index.html` 防闪烁脚本修正 persist 形状 `{state:{mode}}`（原读 .theme 导致深色首帧闪白）；`AppShell` 加跳转链接（preventDefault + 手动 focus main）/`<main id="main" tabIndex=-1>`/`nav aria-label`；`ThemeToggle` role=group + 每钮 aria-label；5 处表格补 aria-label 与 `th scope=col`；4 处判分结论区加 `role=status aria-live=polite`；Markdown 表格可访问名取上方最近标题；3 处点击目标撑到 ≥24px（空位跳转钮 / 阅读题「空格显形」/ 判分实验室降级勾选）；`acceptance-stage10.mjs` 并入 `auditA11y` 体检函数与 a11y / perf 两个新套件、375px 套件加强到 16 页 | `npm run acceptance:stage10` 59/59 全绿（a11y 9：17 页 × 深浅两主题 0 无名控件 / 0 对比度不足 / 0 表格缺陷 / 0 目标过小 / 每页 1 个 h1，键盘 Tab→跳转链接→main、列表→详情、播放器 空格/←/→/Home/End，单选判定 live region；perf 7：首页 0 详情语料 + 3 个 JS 且不含播放器/填空编辑器/Markdown 表格，进演示页才拉 Player chunk 与 data/viz/{id}.json，进填空题才拉 CodeMirror chunk，进题目只拉一片分片；mobile 加强：16 页无溢出、宽表格容器内横滚、提交钮完整在视口、单步真走一步）；typecheck / build / verify:pages 通过
- 2026-09-11 | 阶段 10-5 全站最终验收 | `scripts/acceptance-stage10.mjs` 扩容至 901 行 / 新增 `player5`（确定性伪随机抽 5 套演示验播放·暂停·单步·后退·重置）与 `category`（c/ds 双类列表筛选计数·详情·关联不跨类·缺失走空状态）两套件；`acceptance-site-ui.mjs` 删除已死的 SectionPage 占位断言与硬编码 518，改按 index.json 现算三板块总数；`acceptance-progress-ui.mjs` 4 处写死 id 改为现扫全部分片动态解析（FB_NO_ANSWER / SA_GAP / CR_GAP / CR_IS_DESC）并新增「索引 defects.codeReadingNoAnswer 与分片现算吻合」断言；新增 `docs/阶段10最终验收报告.md`（377 行，含验收矩阵、用户 10 项逐条对照、四道题真 Godbolt 实测输出、37 道未验证 code_reading 成因拆解、部署配置、后续建议、复现命令） | 7 套 UI 验收合计 275 项失败 0：site-ui 16/16（四道指定题真打 Godbolt 12 次串行：pg-001 3/3、cr-043 stdout 逐行一致、cc-001 Tab 循环 1>2>3>1 且 3/3、dbg-001 0/3→3/3；已通过 F5 后仍在）、list-ui 39/39、progress-ui 76/76（导出→清空→导入逐字节一致）、viz-ui 51/51（gen:viz 34 套/619 步字节级可复现）、dbg-ui 21/21、subpath 9/9、stage10 63/63；typecheck / build / verify:pages 全绿，verify:data 题目总数 518 · error 0 · warn 37（28 教材片段无 main + 4 依赖 argv/磁盘文件 + 5 answerIsDescription，无 VERIFIED-NO-PROOF）；全流程控制台 0 error 0 warn 0 pageerror；按要求未 push
