# 阶段 7-9 可视化验收报告

> 验收日期：2026-09-11 · 分支：main · 验收方式：全自动脚本（Playwright + 数据校验），无人工放行项

---

## 一、产出清单

### 1.1 演示语料：34 个 / 619 步（要求 ≥30 ✅）

全部由 `npm run gen:viz`（scripts/gen-viz.ts + src/modules/viz/algorithms/* 生成函数 + VIZ_SPECS 注册表）一条命令生成，
**字节级可复现**（验收内置重跑 sha256 对比：35 个 JSON 差异 0 个），无手写 JSON。

| 类别 | 渲染器 | 数量 | 总步数 | 演示 id |
|---|---|---|---|---|
| 排序 | R1 柱状图 BarsRenderer | 8 | 348 | bubble / selection / insertion / shell / merge / quick / heapsort / radix |
| 线性表 | R2 节点链 NodeChainRenderer | 6 | 53 | singly-insert / singly-delete / doubly-insert-delete / stack-push-pop / queue-enqueue-dequeue / circular-list |
| 树 | R3 树 TreeRenderer | 8 | 94 | preorder / inorder / postorder / bst-insert / bst-delete / heap-build / avl-rotate / huffman |
| 图 | R4 图 GraphRenderer | 6 | 87 | bfs / dfs / dijkstra / prim / kruskal / toposort |
| C 内存 | R5 内存格 MemoryRenderer | 6 | 37 | swap-call / pointer-address / array-layout / call-stack / multi-pointer / malloc-free |

每步为完整状态快照（steps[].snapshot），带解说；生成期自检通过：n ≤ 50 / 每步有解说 / kind 与 renderer 一致 / 统计口径吻合。
步骤数与理论复杂度吻合（抽验：冒泡 n=8 比较 28 = n(n-1)/2 ✅）。

### 1.2 双向关联回填（scripts/backfill-viz-links.ts 规则匹配，仅动 relatedViz / vizIds 两字段）

| 侧 | 回填数 | 说明 |
|---|---|---|
| 知识卡片 relatedViz | 58 / 345 | 覆盖 c-ch06/07/09/10、ds-ch02~08；无关联演示的卡片保持 []，未硬填 |
| 题目 vizIds | 59 / 518 | 分布：memory-pointer-address 32、memory-swap-call 10、memory-array-layout 8、memory-call-stack 6、memory-multi-pointer 6、linear-singly-insert 5、memory-malloc-free 3、linear-doubly/dingly-delete、sort-bubble 各 1 等 |

判分逻辑与题目其它字段零改动（git diff 复核：仅追加 vizIds 字段）。

### 1.3 页面接线

- `src/pages/ProblemDetailPage.tsx`：新增 RelatedVizLinks 区块（题目 → 关联演示跳转，`data-role="related-viz"`），索引加载失败静默跳过，无 vizIds 不渲染。
- 演示列表页 / 演示播放页 / 多算法对比页：阶段 7-8 已就绪，本阶段升级验收到 34 演示口径。

---

## 二、验收结果：51 / 51 项全部通过（`npm run acceptance:viz-ui`）

| 验收项 | 结果 |
|---|---|
| 索引收录 34 个演示（≥30），索引与语料文件一一对应 | ✅ |
| 8 种排序 renderer/category/steps 与语料本体一致 | ✅ |
| 8 种排序逐一：打开即第 1 步 / 播放 / 暂停 / 键盘 →← 单步 / 空格 / 进度条末步（计数器与语料一致）/ 重置 | ✅ 32 项 |
| 冒泡 n=8 比较次数 = 28，与理论复杂度吻合 | ✅ |
| 随机抽 5 个演示（R2 singly-insert、R2 queue、R3 inorder、R4 dijkstra、R5 malloc-free）：播放/单步/后退/末步/重置全可用，总步数与语料一致 | ✅ |
| 五类渲染器深/浅色主题均正常渲染（CSS 变量适配，深色下 R2/R3/R4/R5 画布逐一验证） | ✅ |
| 列表页 34 张演示卡片按渲染器分类陈列，id 与索引一致 | ✅ |
| 题目详情页 → 关联演示：c-ch09-cc-001 展示 2 个关联演示链接，点击落在 #/viz/memory-call-stack | ✅ |
| 对比页：4 面板同步步进 / 计数与语料逐位相符 / 播放到末步标「已结束」/ 换算法进度不丢 / 重置归零 | ✅ 6 项 |
| npm run gen:viz 一条命令重生成全部语料，退出码 0 且字节级可复现 | ✅ 差异 0 |
| 全流程控制台 error / warn / pageerror 为空 | ✅ 0 / 0 / 0 |

**卡片详情页跳转 = N/A**：`/knowledge` 目前为 SectionPage 死配置（既定约定不动），无卡片详情页可测；
卡片侧 relatedViz 数据已回填完毕，待阶段 10 实装知识详情页后直接可用。

### 配套命令全绿

| 命令 | 结果 |
|---|---|
| npm run typecheck | ✅ 0 错误 |
| npm run build | ✅ 329ms，产物正常 |
| npm run verify:pages | ✅ 全部资源相对路径，dist 可放任意子目录 |
| npm run verify:data | ✅ error 0 · warn 37（均为既有 NOT-VERIFIED 代码题待 Godbolt 实机验证，本会话按铁律不跑判分链路，非本阶段引入） |

---

## 三、本阶段对验收脚本自身的修复（scripts/acceptance-viz-ui.mjs）

1. check0 改为动态枚举语料文件（≥30 + 索引与文件一一对应），sec4 改为抽 5 演示全控制，sec5 卡片数动态化——升级至 34 演示口径；
2. 修复 sec5b 代码块重复插入导致的 `Identifier 'shard' has already been declared`；
3. 修复 sec5b 导航到题目详情页后，sec6 在错误页面找对比入口导致的 click 超时（对比段前先回 #/viz）；
4. 修复异常路径下 browser 未关闭导致 node 进程挂死不退（browser 提升模块作用域 + finally 关闭 + 显式 process.exit）。

---

## 四、遗留与下一步

- warn 37（NOT-VERIFIED 代码题）：属判分链路遗留，待跑 `npm run judge:verify`（需 Godbolt，本会话按铁律未跑）。
- 阶段 10（新会话）：三向联动打磨（知识详情页实装后接通 卡片→演示→题目→卡片 闭环）+ 最终验收。

## 五、复验命令

```powershell
npm run typecheck; npm run build
npm run gen:viz; npm run build:index
npm run acceptance:viz-ui   # 51 项 UI 验收
npm run verify:pages; npm run verify:data
```
