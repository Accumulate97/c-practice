# 模块 4 验收报告 · 程序改错（debug）

日期 2026-09-09 · 后端 Godbolt cg132（gcc 13.2，`-std=c99 -Wall -Wextra`，gcc 缺省 -O0）· 软超时 25 s · **严格串行**（111 次真实请求，0 并发）。
测试题：15 道已就绪 debug 题（`c-ch04-dbg-001~003` / `c-ch05-dbg-004~006` / `c-ch06-dbg-007~009` / `c-ch07-dbg-010~012` / `c-ch09-dbg-013~015`），全部 `verified: true`（由 `judge:verify` 置位，本模块一个字节未改）。

---

## 一、交付清单

| 文件 | 负责什么 |
|---|---|
| `src/modules/problems/renderers/DebugRenderer.tsx`（新增，17.2 kB 源） | 展示带 bug 代码（整段可编辑）→ 提交后**复用模块 1 的 `stdin-run.ts`** 串行跑 testCases 比对输出；三级提示阶梯；答对后揭示 `fixed_code` 与 `explanation` |
| `src/pages/ProblemDetailPage.tsx` | 按 type 分派，接上 `debug`；占位符收敛为「阶段 4 模块 5」 |
| `scripts/acceptance-dbg.ts`（`npm run acceptance:dbg`） | Node 侧实机验收：Vite SSR 加载应用**真实判分链路**，5 类场景 37 项判定 |
| `scripts/acceptance-dbg-ui.mjs`（`npm run acceptance:dbg-ui`） | 浏览器侧 UI 验收：playwright-core + 本机 Chrome 跑 dist 产物，21 项检查 |
| `docs/待办-backlog.md` | 新增待办 3（bugs 文本行号缺陷，见下节） |

判分**没有新写一套**（用户明令）：`createJudgeClient` / `toProblemTestCases` / `runStdinTests` 全部来自
`src/modules/problems/grading/stdin-run.ts`，状态文案来自 `src/modules/problems/verdict-meta.ts`。
DebugRenderer 只负责呈现与教学提示，一行判分规则都不写。
已知重复：`CaseCard` / 输出对比块的呈现与 `ProgrammingRenderer` 同构但未抽公共组件（两处呈现细节正在分化，
过早抽象成本更高）——留给模块 5 之后视需要收敛。

---

## 二、系统性数据缺陷披露（15 道 debug 题里 14 道的 bugs 行号写错）

**这不是验收脚本的锅，是题库内容缺陷，必须披露。**

发现过程：harness 构造「只保留一处 bug」的部分修正时，按 bugs 文本行号把 `fixed_code` 的对应行换回原样，
结果换回去的是一条**没有 bug 的行**，代码退化成完全正确 → 3/3 全通过 → partial / partial-scan 假 FAIL。
追根因做了全量扫描：逐行**大小写敏感**比对每题 `code` 与 `fixed_code`，对照 bugs 文本开头的「第 N 行」。

> 扫描踩坑记录：第一轮用 PowerShell `-ne` 比对漏检（`-ne` 大小写不敏感，`z`→`Z` 这类修正被当成相同行），
> 换 `-cne` / JS `!==` 重扫后才是下表。

| 题号 | 处数 | bugs 文本声称 | 真实差异行 | 命中 |
|---|---|---|---|---|
| `c-ch04-dbg-001` | 2 | 7、9 | 6, 8 | 0/2 ❌ |
| `c-ch04-dbg-002` | 2 | 9、11 | 7, 9 | 1/2 ❌ |
| `c-ch04-dbg-003` | 3 | 7、9、10 | 6, 9, 10 | 2/3 ❌ |
| `c-ch05-dbg-004` | 2 | 5、11 | 4, 11 | 1/2 ❌ |
| `c-ch05-dbg-005` | 2 | 9、10 | 7, 8 | 0/2 ❌ |
| `c-ch05-dbg-006` | 2 | 8、10 | 7, 9 | 0/2 ❌ |
| `c-ch06-dbg-007` | 2 | 9、11 | 7, 10 | 0/2 ❌ |
| `c-ch06-dbg-008` | 2 | 10、9 | 8, 9 | 1/2 ❌ |
| `c-ch06-dbg-009` | 3 | 8、9、10 | 8, 10, 11 | 2/3 ❌ |
| `c-ch07-dbg-010` | 2 | 2、12 | 2, 4, 5, 6, 12 | 2/2 ✅ |
| `c-ch07-dbg-011` | 2 | 5、12 | 5, 11 | 1/2 ❌ |
| `c-ch07-dbg-012` | 2 | 7、9 | 6, 8, 15 | 0/2 ❌ |
| `c-ch09-dbg-013` | 2 | 11、10 | 8, 10 | 1/2 ❌ |
| `c-ch09-dbg-014` | 2 | 4、11 | 5, 12 | 0/2 ❌ |
| `c-ch09-dbg-015` | 2 | 8、7 | 8, 9 | 1/2 ❌ |

- 影响面：bugs 是**第 3 级提示的正文**，`explanation` 里也复述了同样的错误行号
  （如 `c-ch04-dbg-001` 解析写「第 9 行方向写反」，实际第 8 行）。**判分完全不受影响**——
  判分只读 testCases 的实机 stdout，一行 bugs 文本都不碰。
- 临时缓解（本模块已落地）：`DebugRenderer` 第 2 级提示的行号**不采信 prose**，改为运行时现算
  `code` ↔ `fixed_code` 的真实差异行并折叠连续区间，学生看到的行号一定对得上编辑器。
- 根治：内容阶段逐题重写 bugs / explanation 的行号，登记为 `docs/待办-backlog.md` **待办 3**，
  排期模块 5 之后。**不需重跑 `judge:verify`**：`code` / `fixed_code` / `testCases` / `expected` 一个字节不动，verified 证据依然有效。

---

## 三、Node 侧实机验收（`npm run acceptance:dbg`）

真实输出（`tmp/acc-dbg2.log`，证据 `tmp/acceptance-dbg.json`）：

```
PASS unchanged     c-ch04-dbg-001 实机 0/3 states=wrong-answer×3
PASS fixed         c-ch04-dbg-001 实机 3/3 states=accepted×3
PASS unchanged     c-ch05-dbg-006 实机 1/3 states=accepted×1,wrong-answer×2
PASS fixed         c-ch05-dbg-006 实机 3/3 states=accepted×3
PASS unchanged     c-ch07-dbg-010 实机 1/3 states=wrong-answer×2,accepted×1
PASS fixed         c-ch07-dbg-010 实机 3/3 states=accepted×3
PASS partial       c-ch04-dbg-001 真实差异行 [6,8]（prose 声称 [7,9]，不符）、只保留第 8 行 → 实机 0/3 states=wrong-answer×3
PASS partial-scan  c-ch04-dbg-002 真实差异行 [7,9]（prose [9,11]）、保留第 9 行 → 实机 1/3（混合：部分用例通过部分失败）
PASS partial-scan  c-ch05-dbg-004 真实差异行 [4,11]（prose [5,11]）、保留第 11 行 → 实机 2/3（混合：部分用例通过部分失败）
PASS partial-scan  c-ch06-dbg-007 真实差异行 [7,10]（prose [9,11]）、保留第 10 行 → 实机 1/3（混合：部分用例通过部分失败）
PASS partial-scan  c-ch07-dbg-010 真实差异行 [2,4,5,6,12]（prose [2,12]）、保留第 12 行 → 实机 1/3（混合：部分用例通过部分失败）
PASS partial-scan  c-ch09-dbg-013 真实差异行 [8,10]（prose [11,10]）、保留第 10 行 → 实机 1/3（混合：部分用例通过部分失败）
PASS syntax        c-ch04-dbg-001 删分号后首组 state=compile-error（应 compile-error 且走正常判分面板）

════ 汇总 ════
  unchanged     PASS 15  FAIL 0
  fixed         PASS 15  FAIL 0
  partial       PASS  1  FAIL 0
  partial-scan  PASS  5  FAIL 0
  syntax        PASS  1  FAIL 0
  真实网络请求 111 次（严格串行），执行合计 66.9 s
  传输层复跑 0 项
  总判定 37 项，失败 0 项
```

逐条对应用户的验收标准：

| 验收标准 | 结果 |
|---|---|
| `c-ch04-dbg-001` 不改代码直接提交 → 判分失败 | ✅ 实机 0/3，`wrong-answer×3` |
| 改对全部 bug → 判分通过（3/3） | ✅ **15 道全部** `fixed_code` 实机 3/3 accepted |
| 只改部分 bug → 部分用例失败 | ✅ partial 0/3；partial-scan 5 道全部「混合」（1/3、2/3），**没有一道在只剩一处 bug 时全组通过** |
| 改出语法错误 → 显示 compile-error 而非崩溃 | ✅ 删分号后首组 `compile-error`，流程正常收尾 |
| bug 版代码本身必须能编译（逻辑错非语法错） | ✅ **15/15 全部编译通过**（unchanged 场景 states 只出现 `wrong-answer`/`accepted`，无一条 `compile-error`），无需报告题号 |
| 传输层抖动 | 0 次复跑（111 请求全绿） |

单批（3 组用例串行）耗时：min 994 ms / avg 1808 ms / max 3027 ms。

数据强度备注（非缺陷）：5 道（`c-ch05-dbg-006`、`c-ch07-dbg-010/011/012`、`c-ch09-dbg-015`）的带 bug 代码
恰好能通过 3 组里的 1 组，整题结论仍是「未通过 1/3」，符合「全组通过才算通过」的既有口径。

---

## 四、浏览器 UI 验收（`npm run acceptance:dbg-ui`）

真实输出（`tmp/acc-dbg-ui.log`，题目 `c-ch04-dbg-001`，dist 产物 + Chrome headless）：

```
  真实差异行 [6,8]  bugs prose 声称 [7,9]
PASS 默认不展示任何提示面板 ｜ data-hint 元素 0 个
PASS 默认不泄露 bugs 原文
PASS 默认不泄露 fixed_code
PASS 默认不泄露错误处数
PASS 编辑器里是带 bug 的原代码 ｜ 首行 #include <stdio.h>
PASS 提示 1：揭示错误处数 ｜ 本题共有 2 处错误
PASS 提示 1 仍不泄露行号与原因
PASS 提示 2：揭示行号面板
PASS 提示 2 行号 = 现算真实差异行 ｜ 需要改动的行：第 6 行、第 8 行。…
PASS 提示 2 不再复述 prose 的错误行号 ｜ prose [7,9] vs 真实 [6,8]
PASS 提示 2 仍不泄露错误原因
PASS 提示 3：揭示错误原因（bugs 原文） ｜ 2 条
PASS 不改代码直接提交 → 判分失败 ｜ 未通过：0/3 组正确，首个失败是第 1 组
PASS 失败给出人话状态「✗ 输出不符」
PASS 失败时仍不泄露 fixed_code
PASS 删分号 → 显示「✗ 编译错误」而非崩溃 ｜ 未通过：0/3 组正确，首个失败是第 1 组
PASS 编译错误面板带诊断行号
PASS 改对全部 bug → 判分通过 3/3 ｜ 判分通过：3/3 组用例输出正确
PASS 答对后揭示 fixed_code 与完整解析
PASS 恢复原始代码按钮有效
PASS 控制台 error/warn 为空 ｜ 共 0 条其它消息，0 error 0 warn 0 pageerror

════ 模块 4 UI 验收汇总 ════
  21 项检查，失败 0 项
```

教学口径落地（用户裁决）：**默认零泄露**——bugs 原文、`fixed_code`、错误处数、行号一个都不出现，
页面只有题干 + 带错代码 + 三级提示按钮；提示 1/2/3 必须逐级点击才展开，且每一级都验了「不越级泄露」。
答对（3/3 accepted）后才出现「答对了！参考修正代码与完整解析」。
截图：`tmp/acc-dbg-hints.png`、`tmp/acc-dbg-fail.png`、`tmp/acc-dbg-compile-error.png`、`tmp/acc-dbg-solved.png`。
控制台 **0 error / 0 warning / 0 pageerror**（连其它类型消息都是 0 条）。
UI 侧另跑了 3 轮真实提交（9 次 Godbolt 请求，与 Node 侧串行分开执行，未并发）。

---

## 五、体积（`npm run build`，2026-09-09）

```
dist/index.html                                   1.24 kB │ gzip:   0.76 kB
dist/assets/index-C7mVjIju.css                   16.95 kB │ gzip:   4.43 kB
dist/assets/rolldown-runtime-hePW80VL.js          0.71 kB │ gzip:   0.42 kB
dist/assets/index-Bx36J64i.js                    34.05 kB │ gzip:  13.41 kB
dist/assets/ProblemDetailPage-FccBfG4z.js        40.85 kB │ gzip:  11.47 kB
dist/assets/react-BiFcBZqf.js                   281.93 kB │ gzip:  89.57 kB
dist/assets/CodeCompletionRenderer-C3uvrX0z.js  441.54 kB │ gzip: 144.31 kB
```

- 模块 4 **未新增任何依赖**（DebugRenderer 用共享的 textarea 版 `CodeEditor`，不引 CodeMirror）。
- 首页主 chunk `index.js` 34.05 kB **逐字节未变**；CodeMirror chunk 441.54 kB **未变**。
- 变化只在已经是路由级 lazy 的 `ProblemDetailPage` chunk：30.66 → 40.85 kB（gzip 9.77 → 11.47 kB），
  即静态引入 DebugRenderer 的成本 +10.19 kB / gzip +1.70 kB，没有失控。

---

## 六、硬约束核对

- [x] Godbolt cg132 + `executorRequest:true` + `filters.execute:true` 双开关；`executeParameters` 在 `options` 层级下
- [x] 响应扁平，无 `execResult` 兜底；`didExecute !== true` 显式抛传输层错误（`src/judge/backends/godbolt.ts`）
- [x] `truncated === true` 走独立 state（不予判分），绝不静默比成 wrong-answer
- [x] gcc 缺省 -O0；软超时 25 s（> Godbolt ~20 s 执行时限）
- [x] **严格串行**：111 次请求 0 并发（harness 逐个 await；`maxConcurrency: 2` + FIFO + `minIntervalMs: 100` 由 client 兜底）
- [x] stdout 规范化 `(j.stdout||[]).map(o=>o.text).join('\n')`（`joinTextLines`，模块 1 既有实现，未改）
- [x] `verified` 只由 `judge:verify` 置位：本模块**没有改任何题目分片**，`git status` 可证
- [x] 题目代码为标准 C（15 道全部实机编译通过）

---

## 七、复现命令

```bash
npm run typecheck        # tsc -b，0 error
npm run build            # 产物见上表
npm run acceptance:dbg   # Node 侧实机验收，37 项判定（约 70 s，111 次串行请求）
npm run acceptance:dbg-ui# 浏览器侧 UI 验收，21 项检查（需先 build；约 13 s，9 次串行请求）
npm run verify:data      # 数据闸门（本模块未改数据，应仍为 0 error）
```

> ⚠️ 两条 acceptance 都会打真实 Godbolt 公共端点，**不要并行跑**（并发会拉低吞吐，见 ADR-0001 §4.3）。
